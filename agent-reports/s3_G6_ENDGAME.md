G2's master assembly names an **unproved** "Lemma X3 (vernier kill)" as the replacement for the top-heavy kill — my Theorem proves something strictly stronger, so I'll flag that coordination point. All deliverables are in place. Final report follows.

## CLAIMS

**CLAIM 1 (Theorem G6-1, the Marginal Displacement Theorem (MDT) — the assignment's b₀-kill, obtained in closed form). PROVED, self-contained, machine-checked. Confidence 0.95.**
For ANY packing of n squares in [0,k]² (any sizes, any orientations), for a.e. x with x mod 1 ∉ ∪ᵢBᵢˣ (the C″ tall-triangle arcs, total ≤ 2Σdᵢsinθᵢ):
**P(x) − N ≤ Σᵢ pᵢ(x)(1 − dᵢsecθᵢ) ≤ n − Σᵢ dᵢsecθᵢ** (= 1 − ε − ρ for n = N+1, ρ := Σdᵢ(secθᵢ−1) ≥ 0). Since P is an integer: **ε + ρ > 0 ⟹ P(x) ≤ N on the whole good set.** Symmetrically in y. Proof = a signed per-line chord ledger: (i) per-line capacity Σ chords ≤ k (disjoint interiors); (ii) Lemma 0 [P] pins every good-phase chord to exactly d secθ; (iii) **Fact 1**: d secθ < 1 ⟹ pᵢ ≤ 1 at good x (middle region has length d(cosθ−sinθ) < cos²θ ≤ 1); (iv) **Fact 2**: d secθ ≥ 1 ⟹ wᵢ ≥ 1 (via cosθ(cosθ+sinθ) ≥ 1 on [0,π/4]) ⟹ pᵢ ≥ 1 always. So every deficit is credited ≤ once and every surplus is charged ≥ once: β − b0sec = n − Σd secθ ≤ 1 − ε **automatically** — the β < 1 hypothesis and the top-heavy escape were artifacts of session-2's positive-part truncation (Lemma A″ pigeonholed one line and dropped the oversize credit). Full proof: `/Users/kylekabasares/Desktop/erdos-106/agent-reports/session3-derivations/G6_DERIVATIONS.md` §1.

**CLAIM 2 (Corollary G6-2: the complete BKU theorem in five lines, one axis, all sizes). PROVED. Confidence 0.95.**
AP packing of k²+2c+1 squares (arbitrary sizes, c ≥ 0): if Σd = N+c+ε, ε > 0, then (all x good, ρ = 0) P ≤ N+c+1−ε, so P ≤ N+c a.e. by integrality; but E[P] = Σd > N+c. Contradiction. **g(k²+2c+1) = k + c/k, arbitrary sizes, single-axis, no capture counts, no product structure, no second pigeonhole.** This closes F1's flagged gap (d ∈ (1,2)) maximally: it deletes the hypotheses of both prior reproofs (F1: all d ≤ 1; S4: b₀ < ε). Consistency audit against the [V] no-go catalogue in §2 of the derivations (no conflict: the gain is P-integrality — max-vs-mean — not first-moment or LP-representable). Verifier should confirm the argument is absent from BKU's paper (`code/session2/bku24.pdf`).

**CLAIM 3 (G6-3 + Theorem S2′ + Corollary S3′: the TOP-HEAVY-AND-DEFICIT-RICH enemy is dead; S3's dichotomy collapses to one branch). PROVED (S2′ inherits S2's one still-unverified ledger step). Confidence 0.9.**
- **G6-3:** every packing with ε > 0 (or ε = 0 with any tilt, ρ > 0) has P ≤ N a.e. on the good set ⟹ some pᵢ = 0 — **Lemma A″ with β < 1 deleted** on the enemy side. Boundary exact: at ε = ρ = 0 the bound is N+1, reproducing the deficient column's full-hit measure k/(k+1).
- **S2′:** every ε > 0 packing satisfies |Av| ≤ Σ(1−wᵢ)₊² + U_x+U_y+V_x+V_y (S2's proof verbatim, its only β-use replaced by G6-3).
- **S3′:** hence unconditionally: **U_x+U_y+V_x+V_y ≥ 2ε + Σ_{w<1}mᵢ + Σ_{w≥1}(1−dᵢ)² for every enemy.** Branch (i) (b₀ ≥ ε+Γ) is DELETED. The assignment's case-(III) target ("kill top-heavy separately… would close case (III) COMPLETELY") is achieved: at AP it is dead outright (Claim 2), at any tilt the b₀-dodge is gone. **Coordination: this proves strictly more than G2's UNPROVED Lemma X3 (vernier kill) in `G2_MASTER_ASSEMBLY.md`; X3 can be retired.** G4's chain theorem was not needed (the assignment's #chains ≥ c₅b₀ heuristic is subsumed: on T12 the would-be c₅ = 1+1/b₀ → 1, so no constant works — the correct object was always the signed global ledger, and MDT is its optimal form).

**CLAIM 4 (β-frontier recomputed — assignment's suspicion CONFIRMED; corrected invariant found). PROVED + machine-checked. Confidence 0.97.**
T12 (k = 2b(b−1)): **β = b, b₀ = b0sec = b−1** (exact, b = 2..5 checked) — "the extremal manifold sits at β = 1" is **FALSE**; β is unbounded on the catalogue. The correct equality coordinate is the MDT bound **n − Σdᵢsecθᵢ = β − b0sec = 1 − ε − ρ, which equals 1 on EVERY known extremal** (U_k(a): 1−0; split cells: 1−0; T12: b−(b−1); deficient row: 1−0), and MDT is exactly tight on all of them: max(P−N) = 1 with |{P=N+1}| = k/(k+1) (U_k), 2/3 (T12 k=4), a (split cell), on ≥ 1 axis (the F6 winding axis). The equality profile (all k lines chord-tiled, every deficit square incident, every surplus square single-incident) is precisely the "bijective near-grid capture + one idle" rigidity the No-Repair theorem demanded — now derived.

**CLAIM 5 (OC-FCMB equality classification — assignment task 1, first half). PROVED. Confidence 0.95.**
hits(p) = #(Λ_p ∩ G) a.e., so multiplicity mass Σ_{m≥2}|{hits≥m}| = g − |π(G)| = the gap's folding-overlap loss; **hits ∈ {0,1} a.e. ⟺ folding injective on the gap ⟺ |π(G)| = g.** Multiplicity-free extremals: tilings (g = 0) and split cells; k-fold folders: U_k (multiplicity (k−1)/(k+1)). The assignment's conjectured reverse OC-FCMB ("multiplicity ≤ (b₀−ε)₊ + …") is **FALSE on U_k itself** (LHS → 1, RHS = 0): multiplicity is a deficit-chain/winding phenomenon, not an oversize one. The proposed chain (b₀ < ε ⟹ mult = 0 ⟹ |Av| ≤ s) is superseded, not repaired: S2′ needs neither multiplicity accounting nor any b₀ hypothesis.

**CLAIM 6 (G6-5: width-surplus localization + taper ledger — new leverage for bridges (I)/(II)). PROVED. Confidence 0.9.**
For enemies: (a) **t = ε+τ ≤ ∫_{∪Bᵢˣ}(P−N)₊** — the entire width surplus (≥ 2ε by T4) is carried on the bad arcs, per axis; (b) everywhere P−N ≤ β + T(x), T = # end-region incidences (T = 0 at good x); (c) integrality: (P−N)₊ ≤ ⌊β⌋ + T, so **ε+τ ≤ E[T; P≥N+1] ≤ 2Σdᵢsinθᵢ when β < 1**, and ε+τ ≤ ⌊β⌋U_x + 2Σdᵢsinθᵢ in general — a single-axis linear tilt cap (T4's order via a disjoint mechanism), and the anti-clustering target now needs to be proved only ON ∪Bᵢˣ. Consistent with the orchestrator's sinθ ≥ σ(θ) fact (verified): mass alone never violates it; only clustering is attackable — and (a) pins where.

**CLAIM 7 (task 2, local rigidity).** Within AP, MDT makes the manifold the set of **global** maximizers — nothing local remains. In tilt-normal directions the sharpest proved statements are the linear caps (T4 and Claim 6c). GAP flagged: true second-order behavior (numerics' 0.427t is order kt; the cap is order Nt — factor-k loose; that factor is exactly bridge (II)'s business).

## CHECKS

`g6_check.py` (scratchpad + `/Users/kylekabasares/Desktop/erdos-106/code/session3/g6_check.py`), all assertions pass: 15 SAT-validated packings (U_k equal/uneven k=2,3, split cell, T12 k=4, deficient row, tilted columns t=0.02/0.05, oversize-tilted with d secθ>1 but no guaranteed midline, fat deficit-chord squares with w>1, random tilted n=12–20), 4001 generic phases each: per-line capacity, chord ledger, MDT good-set bound, Facts 1–2 per-square (adversarial fat square attains p=2 on 15.35% of phases — all inside its own B-arcs, zero good-set violations), integer taper ledger, E[P]=Σwᵢ (5e-4), sharpness values exact (0.6668/0.7501/0.3702/0.6668), T12 arithmetic b=2..5, sinθ ≥ σ(θ), reverse-OC-FCMB counterexample arithmetic. Honest limitation: the ε>0 conclusions are untestable on real packings (none exist iff the conjecture holds); every link is what's machine-checked.

## DEAD ENDS

1. **Reverse OC-FCMB / multiplicity-vs-b₀** (assignment's route): false on U_k; retire.
2. **Chain-counting with c₅ > 1**: no such constant exists (T12: demand/(b₀) → 1); the signed ledger replaces it.
3. **45°**: MDT vacuous there (good set empty), as for every counting-family statement; unchanged jaw assignment.
4. **MDT as a full-conjecture closer**: the mean form of Claim 6 is first-moment-neutral (2Σd sinθ ≥ 2τ ≥ 2ε always); the residual gap is still the union-measure/anti-clustering statement — now the *only* gate.

## BEST NEXT STEP

Propagate into `ERDOS_106_REPORT.md` §7C: MDT (new theorem), BKU-in-five-lines, S2′/S3′ (delete S3 branch (i) and G2's Lemma X3), corrected β-frontier (β−b0sec = 1, not β = 1). Then attack the last gate in its newly narrowed form: **prove |∪ bbox-extreme arcs| + |∪ V-arcs| < 2ε + margins is violated only by packings paying gap ≥ 2c**, using Claim 6(a)'s localization (surplus lives ON the arcs) + α′-edge; the natural first move is the F_i ⊆ Bᵢˣ overlap lemma already flagged in `fp_P_RIGIDITY.md`'s Best-Next-Step, which now composes with S3′ instead of the two-branch S3. An independent verifier pass on MDT (Facts 1–2 and the a.e./genericity bookkeeping) and on S2's inherited ledger step should be commissioned first — MDT is load-bearing for everything above.

Files: derivations `/Users/kylekabasares/Desktop/erdos-106/agent-reports/session3-derivations/G6_DERIVATIONS.md` (copy of scratchpad/session3/G6/); code `/Users/kylekabasares/Desktop/erdos-106/code/session3/g6_check.py`.