## CLAIMS

All code, tables, and full derivations: `/private/tmp/claude-501/-Users-kylekabasares-Desktop-erdos-106/6b0597a9-48bd-4e8c-a957-7fc4a05f26d8/scratchpad/session2/F3/` (see `DERIVATIONS.md`, `algebra_check.py`, `rig.py`, `famA.py`, `famBC.py`, `tests2.py`). Notation: `c = cos t + sin t ∈ (1, √2]`, `d' = d/c` (inscribed AP side), `e = g+s−1` (excess), `foldloss(G) = g − |π(G)|`.

**CLAIM 1 (THE assignment's #1 algebra check — the naive reduction FAILS, decisively and exactly).** For the inscribed-square replacement `d → d' = d/c`:
`Δs + area(S\S') = [(1−d')² − (1−d)²] + (d² − d'²) = 2d(1 − 1/c) > 0` for every `t ∈ (0, π/4]`, `d > 0`.
So `s' + area(R) ≤ s` is FALSE always; the shortfall is exactly the **perimeter defect** `2(d − d')`. Worse: `Δs = s'−s = d(1−1/c)(2 − d(1+1/c)) > 0` alone for all `d < 2c/(c+1)` (and `2c/(c+1) > 1`, so all `d ≤ 1` qualify). Since plain AP-FCMB provides zero slack, the route fails **even if the comparison error `|Av\Av'|` were 0**. No sharpening of the corner-region bound can rescue it; a *slack-carrying* AP statement is necessary. Sympy-verified (`algebra_check.py`, both identities return True). The hoped 3-line induction "full FCMB ⟸ AP-FCMB" is dead as stated. Confidence: proven.

**CLAIM 2 (Comparison Identity Theorem — proved, any orientations, any number of tilted squares, no one-point-capture assumption).** Let P' replace every tilted square by its concentric inscribed AP square (side `d_i/c_i`; containment proof in DERIVATIONS D2), `R = ∪(S_i\S_i')`. Then (i) P' is a valid AP packing, `G' = G ⊔ R`; (ii) using only the budget `C ≤ N` a.e.: `Av' = Av \ π(R)`, hence **`Av' ⊆ Av` and `|Av| − |Av'| = |π(R)\π(G)|` exactly**; (iii) exchange identity `(s'−s) + |R| = 2Σ(d_i − d_i')`; (iv) **Master equivalence: FCMB(P) ⟺ slack_AP(P') ≥ (s'−s) + |π(R)\π(G)|**, where `slack_AP(Q) = s(Q) − |Av(Q)|`, and the RHS is always `≤ 2Σ(d_i − d_i')`. Numerics: `Av' ⊆ Av` violations 0.0 and `|Av\Av'| = |π(R)\π(G)|` to grid precision in all ~100 runs. Note the orchestrator's directional worry resolves cleanly: `C' ≤ C` pointwise gives `Av' ⊆ Av`, which is the *useful* direction. Confidence: proven.

**CLAIM 3 (foldloss form of FCMB — sharpest reformulation).** `|Av| = 1 − |π(G)|` exactly (any orientations), hence `s − |Av| = e − foldloss(G)` and **FCMB(P) ⟺ foldloss(G) ≤ e = g+s−1**: the self-overlap of the folded gap is bounded by the packing's excess. FCMB ⟹ conjecture is now trivial (`foldloss ≥ 0`). Numerically confirmed (family B: fold = 0.18361, e = 0.2401, margin = 0.0565 = e − fold). Consequence: **Claim 2's reduction is an exact change of coordinates, not a strict decrease of difficulty** — the required slacked-AP statement is FCMB(P) itself rewritten. Its real value is Claims 1 + 7: it proves plain AP-FCMB insufficient and isolates where slack must come from. Confidence: proven (identity is 2 lines).

**CLAIM 4 (Tilt-neutrality of |Av| — corrects a session-1 belief).** For a tilted square with no two points at integer-vector difference (e.g. diam < 1), `|π(S)| = d²` independent of t. In configurations with clearance, tilting a square about its center *without shrinking* leaves `|Av|` **exactly unchanged** (proved on the cell family; numerically constant to 6e-4 on a 700² grid across t ∈ [0, π/4]). Session-1's "tilted perturbations strictly decrease |Av|" is 100% an artifact of the *forced shrinkage*, 0% a capture-measure effect. Any proof of tilted FCMB must exploit packing-validity geometry, not the tilted square's capture measure. Confidence: proven.

**CLAIM 5 (Unconditional one- AND two-tilt FCMB on the entire tightness manifold — the new theorem).** Let P = (N−1) axis-parallel unit squares on lattice cells + two squares `S_a, S_b` (sides a, b, **arbitrary orientations**) with disjoint interiors inside the remaining cell. Then
**`|Av| = a² + b²` and `s − |Av| = 2(1 − a − b) ≥ 0`,** i.e. FCMB holds, with equality iff `a + b = 1` (by k=1 sharpness: forces the AP split-cell family).
Proof (complete, DERIVATIONS D6): each unit cell square captures exactly 1 point for *every* p; the open remaining cell holds exactly one lattice point q(p) a.e.; S_a, S_b ⊆ cell are disjoint so `Av = {q∈S_a} ⊔ {q∈S_b}` with measures a², b² (cell-confined folding is injective); finally `a+b ≤ 1` is exactly the theorem `f(2)=1` (any orientations) applied in the cell. ∎
This is the first FCMB theorem beyond k=1 that covers tilted squares, and it covers all tilted deformations of the *entire known tightness family* — where the danger lives. Verified: 25 random tilted placements (`tests2.py`, 0 mismatches) + 24-point (a,t) scan of the shrink-to-fit family with margin `= 2a(1−1/c)` matching to ≤1.5e-3 (`famA.py`). Confidence: proven.

**CLAIM 6 (Grain corollary — free).** Claims 2–3 never use the number or coherence of tilted squares nor single-point capture. For an m×m coherent grain at tilt t the same Master equivalence holds verbatim with total perimeter defect `2(1−1/c)·Σ_grain d_i`. The formula `μ(t,m)` is not needed for the reduction route (only for a direct capture-event attack). Confidence: proven (as a corollary; no new unconditional grain theorem — see GAP-3).

**CLAIM 7 (Two-regime slack structure — where the missing budget lives; numeric).** The two loss terms of the reduction, `Δs` and `|π(R)\π(G)|`, are covered by two distinct slack sources, each observed *separately sufficient*:
- Tight one-cell regime (family A): `π(R) ∩ π(G) = ∅` (corner term maximal `= |R|`), but P' carries side-sum deficit slack `2(a − a/c²) >` requirement `2(a/c − a/c²)`.
- Folding regime (family B, two congruent near-unit squares at offset (1,0)): measured `π(R) ⊆ π(G)` **exactly** (corner term = 0.0000 in all 6 configs, absorbed completely by the folded gap); requirement reduces to `Δs`, supplied by `e − foldloss` slack.
Plus 60/60 random tilted perturbed split-cells: min FCMB margin 0.257, zero violations, zero budget (`C>N`) violations. Confidence: numeric, robust.

## COUNTEREXAMPLES AND CHECKS
- `|Av| ≤ 1 − g` (no-folding strengthening of AP-FCMB): **FALSE** — family B has `|Av| = 0.810 > 1−g = 0.626`. It holds precisely on cell-confined-gap configs.
- "Tilting decreases |Av|": FALSE as a mechanism (Claim 4); true only through forced shrinkage.
- All identity checks (structure identity conservation `2(d−d') = ΔareaR + Δs`, `Av'⊆Av`, `|Av\Av'|=|π(R)\π(G)|`, `red_ok ≡ margin`) pass to grid precision on 600²–700² grids across families A, B, C.

## DEAD ENDS
1. Naive inscribed-replacement induction (Claim 1) — fails by exactly `2Σd_i(1−1/c_i)`; irreparable under plain AP-FCMB.
2. Any FCMB proof routed through `|π(S_tilted)| < d²`: false premise, capture measure is tilt-invariant (Claim 4).
3. Ratio/area-only accounting of the corner regions R: the structure identity converts perimeter to gap+deficit at exactly break-even; only *positional* information (`π(R)∩π(G)` absorption, Claim 7) or side-sum deficit can pay.

## BEST NEXT STEP
Extend Claim 5 to a *soft sea* (units relaxed to sides `1−ε ≤ d_j ≤ 1`): the exact obstruction is that sea squares no longer capture w.p. 1. This is precisely F1's AP machinery plus Claim 2's corner accounting, and by Claim 7 the needed slack should be sourced regime-by-regime: prove **foldloss(G) ≤ e for AP sea + one tilted square** by showing that whenever the corner term is positive (gap near one cell, family-A-like), the tilted square forces a quantifiable side-sum deficit `2Σ(d−d/c)` in its cell (an `f(2)=1`-with-tilt stability estimate: two squares in a unit cell, one at tilt t, satisfy `a + b ≤ 1 − κ(t)a` for explicit `κ > 0` — the natural sharpening of Claim 5, and my top candidate lemma). Whoever holds F1: target AP-FCMB in the *slacked* form `|Av| ≤ s − max(0, stuff)`, since Claim 1 proves the unslacked form cannot feed the tilt induction.