## CLAIMS

**C1 — One-axis sufficiency audit: CONFIRMED (assignment part 1).** Traced C″, S2, S3(ii), Theorem W, κ-coupling, α′-edge against an x-clustered / y-scattered enemy: C″ needs only max(U_x,U_y) ≥ 1 − Σ(1−w)_+ (one big axis suffices); S2/S3(ii) use the SUM U_x+U_y+V_x+V_y (U_y ≈ 1 from scattered y-phases satisfies them); Theorem W needs PH on both axes (inapplicable); κ-coupling satisfied via U_y; α′-edge cheap at whisper tilts. So before this session the running-bond enemy complied with every requirement in the arsenal. Full table in §5 of the derivation file.

**C2 — Lemma H (Helly-midline chord lemma) [PROVED, no genericity needed].** Call a set Q of squares a *narrow class* if all x-projections lie in one interval of length X and every member satisfies d_i(cosθ_i − sinθ_i) > X/2 (= w_i − 2d_i sinθ_i, the middle-region length from Lemma 0, now [V] per G1). Then the vertical line at the interval midpoint passes through the interior of every member's middle region; the open chords are pairwise disjoint (disjoint interiors), each of length exactly d_i secθ_i, all inside height k. Hence **Σ_{Q} d_i secθ_i ≤ k.** An interval of length > X/2 inside a length-X interval strictly contains the midpoint — that is the whole proof. 45°-squares can never join a narrow class (condition reads 0 > X/2), so the diamond chain is automatically excluded.

**C3 — Theorem SD, the Columnar Kill [PROVED — main deliverable].** Any packing (any n, sizes, orientations) partitionable into ≤ k narrow vertical classes (class intervals may overlap; only membership partitions) satisfies Σd_i ≤ Σd_i secθ_i ≤ k·(#classes) ≤ N, i.e. **ε ≤ −Σd_i(secθ_i − 1) ≤ 0: the Erdős conjecture holds for all columnar packings, with tilts strictly penalized.** Same with ≤ k narrow horizontal classes, by symmetry. Exactly tight (Σd = N, all class sums = k) on the entire extremal catalogue: deficient column U_k and its whole manifold, T12 column tilings, grid, split cells — machine-verified. The X/2 constant is sharp (two-abreast squares defeat any relaxation).
- **Corollary SD-RB: the running-bond enemy is dead outright, TOP-HEAVY INCLUDED.** Straight x-walls ⇒ k width-~1 columns ⇒ columnar (narrowness holds amply for near-unit whisper squares) ⇒ Σd ≤ N. No phases, no β-dichotomy, and — beyond what the assignment hoped — **the b0 ≥ ε dodge does not exist for columnar packings**: the conclusion is ε ≤ 0, not "β ≥ 1". This confirms and rigorizes the assignment's "strip decomposition kill", with the correct catch made precise: strips are replaced by narrow *classes* (chord/Helly, no y-stacking or y-disjointness needed — that step in the assignment sketch is FALSE for tilted squares and is bypassed entirely).
- **Lemma H+ (weak form):** a single narrow class with ≥ k+1 members forces Σ(1 − d secθ) ≥ 1, i.e. β ≥ 1 ⟺ b0 ≥ ε + Γ. This is the spatial, phase-free version of Lemma A″'s over-full line.

**C4 — Theorem OF (spatial over-full chain theorem) [PROVED — the assignment's conjectured theorem, in stronger Helly form].** t = Σw − N > 0 (every enemy has t ≥ 2ε) forces ∫_{X_over}(n−k) ≥ t, |X_over| ≥ t/(n_max−k) ≥ ~ε/(1.2k) for near-unit packings, and at each over-full x a clique of ≥ k+1 squares with a COMMON abscissa, pairwise disjoint chords, and chord-deficit Σ(1 − ch_i(x)) ≥ n(x) − k ≥ 1.

**C5 — Theorem CD (taper dichotomy) + Theorem CH (few chains).** β ≥ n(x) − k − |T(x)| at every over-full line (T = taper-crossers within d sinθ of a bbox corner). So β < 1 ⇒ **every over-full line hosts a "wedge": a square's extreme corner physically inserted into the (k+1)-stack.** And m pairwise-disjoint middles-only cliques force β ≥ m ⟺ **b0 ≥ ε + Γ + (m−1)**. AP corollaries (taper empty): any AP packing with ε > 0 has b0 ≥ ε (one-paragraph, BKU-free, spatial); and b0 < 1 + ε confines X_over to ONE column of width ≤ max d — the positional deficient column is unique.

**C6 — Mass-neutrality no-go [PROVED].** Exact identity: ∫(1−ch)_+ = w − d² (d ≤ cosθ) or sinθcosθ (d ≥ cosθ); integrating OF is implied by the area bound — vacuous; and taper mass 2Σd sinθ ≥ 2τ ≥ t + ε always suffices to cover X_over. Confirms the orchestrator's strategic fact spatially: only wedge LOCATIONS, class CARDINALITIES and clique DISJOINTNESS carry content, never integrated measure.

**C7 — Non-columnar enemy structure (component dichotomy).** Enemy ⇒ non-columnar on BOTH axes ⇒ per axis either (a) r ≥ k+1 disjoint occupied columns, whence one small member per column gives **s ≥ (r−k)²/r ≥ w_min/k — kills the sub-C-S enemy for all k ≲ (1−√2c)/(2c)** (k ≤ 185 at c = 1/400; a sketched refinement reaches O((2c)^{−3/2}) ≈ 2800, GAP-flagged; for the full conjecture this branch is the transposed deficient manifold, honestly open); or (b) a wide interlocked component (span ≥ ~2) on each axis, which with C5 makes the residual enemy a **wedged running bond**: Ω(k) wedge corners embedded in over-full stacks (whisper tapers have width ~t₀, coverage of |X_over| ≥ ε/1.2k needs ≥ ε/(1.2k t₀) = Ω(k) wedges at the T4 tilt floor).

**C8 — Bridge impact.** (II)→(cluster) is REPLACED by the cheaper "(II)→columnar": coherence need only imply wall wander < 1 − O(√c) (an O(1) demand), not mod-1 clustering to λ = O(√c) — strictly weaker than the old Rigidity Lemma target. (III)'s columnar branch is closed (no b0 dodge); its non-columnar residue is the single-wedged-column near-AP enemy. Secondary winding item: chain winding spreads only the chain's own 2(k+1) arcs (~2kt₀ mass) — no C″ compliance from the chain alone; noted, matches F6-C5.

## CHECKS
`g4_check.py` — all PASS: chord formula (300×40, err 1.4e−14); ∫(1−ch)_+ identity (400 squares, err 6e−10); Lemma H midline (363 random narrow groups incl. tilts to 0.6, 0 failures); SD equality on U_k (k=2,3,4) and T12 (k=4), exact; adversarial 2-class k=2 search (4000 SAT-disjoint trials, best Σd = 3.333 < 4); over-full existence (|X_over| = 0.79, ∫(n−k)_+ ≥ t). Self-attack log in §9 (sharpness of X/2, 45°, oversize, mixed sizes, measurability).

## DEAD ENDS
Integrated/mass forms of the chain argument (exactly neutral, C6); window-covering pigeonhole for the narrow-class number (loses O(√c·k) ≫ 1); taper-mass covering exclusion (mass always sufficient); winding beyond the swept annulus; "Σ_comp d ≤ kX_m" as a standalone lemma (conjecture in disguise). The assignment's y-stacking route for tilted strips is unsound as sketched (x-overlap ⇏ y-disjoint bboxes for tilted pairs) — replaced by the chord/Helly mechanism, which needs no stacking.

## BEST NEXT STEP
**The Wedge Displacement Lemma:** two near-unit stack members abutting a wedged corner of taper width δ have their middle-interval midpoints (hence x-phases) separated by ≥ δ − O(local gap). With the Ω(k) wedge count (C7), this converts position into the first derived phase-scatter bound U_x = Ω(ε/k), to be fed into κ-coupling + S2 on the other axis — the positional replacement for the mod-1 Rigidity Lemma. Fully local, finite, machine-checkable.

Files: `/Users/kylekabasares/Desktop/erdos-106/agent-reports/session3-derivations/G4_DERIVATIONS.md` (full proofs), `.../G4_g4_check.py`, `/Users/kylekabasares/Desktop/erdos-106/code/session3/g4_check.py`; scratch at `/private/tmp/claude-501/-Users-kylekabasares-Desktop-erdos-106/6b0597a9-48bd-4e8c-a957-7fc4a05f26d8/scratchpad/session3/G4/`.