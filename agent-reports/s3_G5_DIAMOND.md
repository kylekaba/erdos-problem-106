## CLAIMS

**The 45° fortress has fallen — twice over.** Deliverable (a) achieved with room to spare, plus full closure to N for k ≥ 28. Derivations: `/Users/kylekabasares/Desktop/erdos-106/agent-reports/session3-derivations/G5_45FORTRESS_derivations.md`; scripts `G5_verify_D_formula.py`, `G5_landscape_and_chain.py` (same dir).

Throughout: n = N+1 squares all at folded tilt exactly 45°, ε = Σd − N, G = N − Σd², s = Σ(1−d)². Master identity (algebraic): **2ε + G + s = 1**.

**Theorem 45-A [proved, unconditional, every k]: Σdᵢ ≤ N + 1/4 + 1/(4(N+1)).** The first sub-Cauchy–Schwarz statement at 45°; c45 = 1/8 uniformly (ε ≤ 3/8 at k=1), → 1/4 asymptotically. Proof: the verified sharpened Wall Lemma (Route-C Claim 6 + p2_V1 audit; coefficient sinθcosθ/2 = **1/4 at 45°, where it is tight**) applied to floor AND ceiling — their gap regions are provably disjoint column-by-column — plus Cauchy–Schwarz over serving squares (m ≤ N+1): G ≥ k²/(2(N+1)); feed into the identity.

**Theorem 45-B [proved]: for k ≥ 28, Σdᵢ ≤ N.** Pure-45 closed to the conjectured value (k=1 closed by T1; open residue only k ∈ [2,27], where 45-A gives ε ≤ 1/4 + 1/(4(N+1))). Proof: assume ε > 0; identity gives G + s < 1, hence all dᵢ < 2 and **at most 3 squares have d ≤ 1/2** (s-counting — this is what kills tiny-diamond wall carpets). Squares with d > 1/2 within height h of the floor number M < 4k(h+2√2) (strip area/area-¼ count). Wall Lemma split (high servers pay h|Aᵢ|, low big servers pay Cauchy–Schwarz S²/4M, ≤3 low small servers serve ≤ 2.13): G_floor ≥ hk − Mh² − 2.13h. At h = 0.04, doubled via the ceiling: **G > 0.0433k − 0.17 > 1 for k ≥ 28**, contradicting G < 1. (Discovery en route: the Window Theorem §4.5 + identity already closes pure-45 for k ≥ 1275 — apparently never noticed; 45-B improves the threshold 45×. Asymptotically the δ-window refinement gives G ≳ 0.11k, so any ε>0 candidate is ~k/9 in forced waste; real rotated-grid packings measure G ≈ 2.2–2.8k.)

**Theorem CO [proved]: all n = N+1 squares at common folded tilt θ, k ≥ 28/sin2θ ⟹ Σd ≤ N.** Same proof, coefficient sin2θ/4, h = 0.04 sin2θ. Extends T3 from Pythagorean-with-c|k to **every angle, including irrational**, at large k. New quantitative bridge for trichotomy branch (I): any common-θ grain at a wall pays sin2θ/4 per squared served length.

**Theorem D45 + landscape [the assigned diamond route, proved]:** In the squares' frame the container is the diamond with vertices (0,0),(k√2,0),(k/√2,±k/√2). Writing k/√2 = K + γ, the discrepancy is EXACTLY

**D(x,y) = K(σ(x+y) + σ(x−y) − 4γ) + X − 2γ²,**

where σ(z) = χ(z)+χ(z−1) with χ(z) = 1_{z mod 2 ∈ [0,2γ)}, X = χ(s)χ(t)+χ(s−1)χ(t−1) ∈ {0,1,2}. Key structural facts: (i) parity factorization — in a=m+n, b=m−n coordinates the diamond count is a two-parity product, giving the closed form; (ii) **σ is 1-periodic** (the two parity arcs are unit-offset copies), so the Θ(K) term, though a function of the diagonal phases, has full period 1 in each — the axis/diagonal phase mismatch costs only a factor 2, not a collapse; (iii) only the O(1) cross term X lives mod 2. Formula machine-verified **exactly, 40,000/40,000** shifts, k ∈ {2,…,41}. BKU chain in this frame: A×B ⊆ {D ≥ 1} where A,B are the P,Q ≥ N+1 pigeonhole sets; **all-short** (dᵢ ≤ 1) gives |A|,|B| ≥ ε. Four regimes in γ = frac(k/√2), all threshold checks **unconditional in K** (no O(1/K) margins — the cross term signs cooperate): γ∈(0,¼]: {D≥1} ⊆ union of diagonal arcs of length 2γ ⟹ ε ≤ 4γ; γ∈(¼,½): intersection regime ⟹ A+B and A−B essentially inside one arc; upgrading to genuine closed sumsets and applying Raikov's circle sumset theorem: ε ≤ γ; γ∈(½,¾]: union, ε ≤ 4γ−2; γ∈(¾,1): intersection, ε ≤ γ−1/2. Sub-C-S for γ ∉ [⅛,¼]∪[⅝,¾]: **density 3/4 of all k** from counting alone (e.g. k=17: ε ≤ 0.083; k=29: ε ≤ 0.024; k=58: ε ≤ 0.049).

**Deliverable (b) — precise obstruction of the pure counting route:** (1) big squares break |A| ≥ ε (honest conditionality of D45; the b0-heavy enemy survives counting exactly as S3 predicted); (2) for γ ∈ [⅛,¼]∪[⅝,¾] the bad arcs are fat and the union bound is vacuous — for those k the diamond route proves nothing beyond T2, consistent with Claim 1(d)/Claim 6; (3) in intersection regimes the enemy can genuinely realize interval-shaped A,B via one coherent miss-arc block, so b(γ) is tight for the information used. The fortress falls to the **wall geometry**, not to counting — confirming the report's α′/T2-jaw prognosis while showing the jaw needed was the Wall Lemma all along.

## CHECKS
D-formula exact 40k/40k; chain Σpᵢqᵢ ≤ k²+D: 0 violations/9,000 shifts on 3 constructed pure-45 packings; Wall Lemma direct quadrature at 45° (LHS 7.537 ≤ ∫φ 7.787 ≤ G 13); identity algebraic; 45-B constants h=0.04: g=0.021642, interior/boundary branches both computed, threshold k ≥ 27.02→28; regime thresholds symbolically re-derived and numerically spot-checked; landscape table for k=2..60 in run output.

## DEAD ENDS
Four-wall factor-4 in 45-A (corner regions overlap; 2 is honest max); unconditional D45 via P_max control (dead per S3); exploiting cross term X (O(1/K) only, enemy-favorable signs on boundary cells); FKG on {D≤0}∩(A×B) (superseded by 1-periodicity of σ reducing everything to 1-D arcs + sumsets).

## BEST NEXT STEP
Close k ∈ [2,27]: per-k constant optimization (δ-window refinement should reach k ~ 12–15), D45 where γ favorable (k=3,5,10,15,17,22 all sub-C-S), SAT/SLSQP certification for k ≤ 6 (numerics already certify ≤ k for k ≤ 4). Strategically: **port CO into trichotomy branch (I)** — the wall-anchor now pays sin2θ/4·|A|² against any grain touching a wall, which is precisely the (I)→(gap) quantitative bridge flagged as order-marginal; and update MEMORY/report §7C: "45° provably out of reach of this counting family" should be amended — out of reach of counting, but closed by walls for k ≥ 28.