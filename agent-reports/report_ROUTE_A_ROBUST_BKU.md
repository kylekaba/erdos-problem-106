All verifications pass (residuals ≈ grid discretization error 3·10⁻⁴; coverage exactly 0 uncovered; E[min(L,B)] = W² and the closed-form margins confirmed). Writing the final report.

## CLAIMS

**Setting/notation.** Scale by k: arena T=[0,k]², M squares S₁,…,S_M with pairwise disjoint interiors, scaled sides dᵢ = k·sᵢ, tilts θᵢ ∈ [0,π/4] (WLOG mod π/2, by the square's symmetry and an axis swap for θ ∈ (π/4,π/2)). N := k². For a shift (x,y) ∈ [0,1)² let Λ = Z²+(x,y); pᵢ(x) = #vertical lines of Λ meeting Sᵢ, qᵢ(y) = #horizontal lines, cᵢ = #(Λ∩Sᵢ). Write u₁ := cosθ+sinθ ∈ [1,√2], σ(θ) := u₁−1, w := d·u₁ (common width of both projections), W := (w−1)₊. A shift is *generic* if no point of Λ lies on any ∂Sᵢ or on ∂T; non-generic shifts form a null set (∂Sᵢ is a finite union of segments; its projection to the torus is null). All counting statements below are at generic shifts; all integrals over the torus are unaffected.

---

**Claim 1 (crossing geometry; exact identity).** Place the bounding box of S at [0,w]². The vertices of S are A=(d sinθ,0), B=(w,d sinθ), C=(d cosθ,w), D=(0,d cosθ), and the box minus S is four right triangles: T_BL = {x,y>0, x/(d sinθ)+y/(d cosθ)<1} with legs (d sinθ, d cosθ), and its images T_BR, T_TR, T_TL under the square's symmetries; T_TR is the image of T_BL under the central symmetry (x,y)↦(w−x,w−y). A vertical line at abscissa a meets S iff a ∈ [0,w] (S is convex with full x-extent), same for horizontal lines; hence the p·q crossings of lines meeting S are exactly Λ∩box, and at generic shifts

  **c = pq − L, where L := #(Λ ∩ (four corner triangles)).**

With B := (p−1)(q−1), the algebraic identity pq = p+q−1+B gives the exact pointwise identity **c = p+q−1+B−L**, hence c ≥ p+q−1−(L−B)₊ whenever B ≥ 0.

*Proof.* Direct computation of the rotated vertices (rotate [0,d]² by θ, translate); convexity of S gives the full-extent statement; additivity of counting over box = S ⊔ triangles. ∎ Confidence: 1.0.

**Claim 2 (sub-problem 3: |p−q| ≤ 1 for every tilt and every shift).** Both projections are intervals of the same length w, and an interval of length w contains ⌊w⌋ or ⌊w⌋+1 points of Z+x. Hence p,q ∈ {⌊w⌋,⌊w⌋+1}, so |p−q| ≤ 1 and B = (p−1)(q−1) ≥ 0 always (the excluded case p=0,q≥2 cannot occur). Confidence: 1.0.

**Claim 3 (sub-problem 1, negative half: pointwise BKU fails for EVERY positive tilt in the relevant width range).** For every θ ∈ (0,π/4] and every d with 0 < w < 2 there is a positive-measure set of shifts with p,q ≥ 1 and c ≤ p+q−2. Consequently the implication [p,q ≥ 1 ⇒ c ≥ p+q−1, all shifts] holds **only at θ = 0** in the regime w < 2, and the assignment's target "BKU verbatim for tilts ≤ η₀" is false for every η₀ > 0: near-extremal packings have w ∈ [1,√2] ⊂ (0,2).

*Proof.* Note d sinθ < 1 always in this range (d sinθ ≤ w·tanθ/(1+tanθ) ≤ w/2 < 1) and d sinθ < w−? Case w ≤ 1: pick u ∈ (d sinθ, w) close to w and v ∈ (0, (u−d sinθ)tanθ); then the unique crossing (u,v) lies in T_BR (below the edge AB, which has slope tanθ through A), so p=q=1, c=0. Case 1 < w < 2: pick u ∈ (0, min(d sinθ, w−1)) if that is nonempty — else pick u ∈ (0, w−1) arbitrary small — and v ∈ (w−1, min(1, d cosθ·(1−u/(d sinθ)))): as u↓0 the upper limit tends to min(1,d cosθ) > w−1 (since d sinθ < 1 ⇔ d cosθ > w−1), so the range is nonempty; then q=1 (v ∈ (w−1,1)), p=2 (lines at u, u+1 ≤ w), and (u,v) ∈ T_BL, so c ≤ pq−1 = 1 < 2 = p+q−1. Both constructions are open conditions, hence positive measure. ∎ Confidence: 0.97 (numerically confirmed at (d,θ) = (0.95,0.02), (1.4,0.02), (1.0,0.3), (0.7,0.3)).

**Claim 4 (sub-problem 1, positive half: pointwise BKU for very wide squares).** If w ≥ 8·(1+sin2θ)/(1−sin2θ) (θ < π/4), then c ≥ p+q−1 for **all** shifts. *Proof.* L ≤ Σ_triangles #(Λ∩bounding rectangle) ≤ 4(d sinθ+1)(d cosθ+1) = 2d²sin2θ + 4w + 4, using d²(1+sin2θ) = w². Budget B = (p−1)(q−1) ≥ (w−2)² since p,q ≥ ⌊w⌋ ≥ w−1. Then (w−2)² ≥ 2w²sin2θ/(1+sin2θ) + 4w + 4 ⟺ w(1−sin2θ)/(1+sin2θ) ≥ 8, and c = p+q−1+B−L ≥ p+q−1. ∎ (Vacuous for packings of k²+1 squares — area forces most sides < 1 — but it completes the (d,θ)-characterization at both ends; the exact frontier for 2 ≤ w ≤ 8(1+sin2θ)/(1−sin2θ) is left open. GAP: mid-range frontier not computed.) Confidence: 0.95 (200k-shift numerical check, 0 violations).

**Claim 5 (defect first moments; the θ² hope is FALSE).** Over the shift torus, E[pᵢ] = E[qᵢ] = wᵢ and **E[L] = area of the four triangles = d² sin2θ**. In particular the per-square defect-event mass is Θ(θ), *not* Θ(θ²): the "both lines in corner wedges" intuition fails because T_BR is short (d sinθ) only in y but long (d cosθ) in x — the defect event is a triangle of area ½d²sinθcosθ per corner, not a product of two θ-wedges. This settles the parenthetical question in sub-problem 4 negatively: surplus 2dσ(θ) ≈ 2dθ and average defect d²sin2θ ≈ 2d²θ are the SAME order; only constants can win. Confidence: 1.0 (standard mean-count identity; Monte Carlo match to 3 decimals).

**Claim 6 (exact algebra of surplus vs defect; criticality at d = 1).** With u₁ = cosθ+sinθ: sin2θ = u₁²−1, and
 (i) 2dσ(θ) − d²sin2θ = d·σ(θ)·(2−d−w); surplus ≥ mean defect ⟺ **d + w ≤ 2** ⟺ d ≤ h(θ) := 2/(1+u₁) ∈ [2(√2−1), 1);
 (ii) σ(θ)² = 2(1−cosθ)(1−sinθ) = sin2θ − 2σ(θ);
 (iii) (w−1)² − dσ(θ)(d+w−2) = (1−d)² identically in (d,θ).
*Proof.* Elementary algebra in u₁ (all three verified symbolically and numerically). ∎ Confidence: 1.0.

**Claim 7 (Coverage Lemma — the budget refinement is exactly computable).** Let θ ∈ (0,π/4] and d ≤ u₁ (then w ≤ u₁² ≤ 2). For every generic shift with (u,v) ∈ [0,W)² (u,v the line offsets in box coordinates; this event has probability W² and forces p=q=2, B=1), at least one crossing is lost: (u,v) ∈ T_BL or (u+1,v+1) ∈ T_TR. Hence **E[min(L,B)] ≥ W²** and
  E[(L−B)₊] = E[L] − E[min(L,B)] ≤ d²sin2θ − W².
*Proof.* The two conditions read ℓ₁ := u/(d sinθ)+v/(d cosθ) < 1 and ℓ₂ := (W−u)/(d sinθ)+(W−v)/(d cosθ) < 1 (using the central symmetry of Claim 1; u+1 ≤ W+1 = w so the crossing exists). Their sum is W·u₁/(d sinθcosθ) = 2u₁(du₁−1)/(d(u₁²−1)) < 2 ⟺ d < u₁; so min(ℓ₁,ℓ₂) < 1. (d = u₁: a.e. shift still works.) ∎ Confidence: 0.97 (grid check: uncovered fraction exactly 0 at five (d,θ) pairs including d=1.15, θ=0.3; E[min(L,B)] = W² to 5 decimals).

**Claim 8 (per-square margin — exact criticality).** Define the per-square margin m(d,θ) := 2w − E[L] + E[min(L,B)] − 2d. For θ ∈ (0,π/4], d ≤ u₁(θ):
  **m(d,θ) ≥ dσ(θ)(2−d−w) > 0 if w ≤ 1;  m(d,θ) ≥ (1−d)² ≥ 0 if 1 ≤ w ≤ 2.**
For θ = 0: m(d,0) = 0 for all d (L ≡ 0). In particular m ≥ 0 whenever d ≤ cosθ+sinθ, with equality **only at d = 1** (w≥1 branch): the conjecture's critical size d=1 is *exactly* the unique tie point of the refined surplus-vs-defect budget. Moreover for d ≤ min(u₁, 1/cosθ) the bound is an equality: E[min(L,B)] = W² exactly (case analysis: the only possible lost crossings on {p=q=2} are (u,v)∈T_BL, (u+1,v)∈T_BR, (u,v+1)∈T_TL, (u+1,v+1)∈T_TR, and T_BL∪T_TR-membership already covers [0,W)²).
*Proof.* Combine Claims 5, 6(i),(iii), 7: 2w−2d = 2dσ; 2dσ − d²sin2θ + W² equals dσ(2−d−w) when W=0 and (1−d)² when w ≥ 1, by 6(iii). Positivity for w ≤ 1: then d ≤ 1/u₁ ≤ 1 so d+w ≤ 2. ∎ Confidence: 0.95 (closed forms reproduced by deterministic quadrature at 8 parameter pairs, residuals ≤ 4·10⁻⁴ ≈ grid error; e.g. (d,θ)=(1.1,0.2): predicted 0.0100, measured 0.01022; (1.15,0.3): predicted 0.0225, measured 0.02247).

**Claim 9 (Main Theorem: tilt-robust BKU with one joint integrality).** Let M squares be packed in [0,k]², and suppose each square satisfies **θᵢ = 0, or dᵢ ≤ cosθᵢ+sinθᵢ, or dᵢ ≥ 3/2**. Then
  **Σᵢ dᵢ ≤ (k² + M)/2 − ½·Σᵢ m(dᵢ,θᵢ)  ≤ (k²+M)/2,**
with m ≥ 0 as in Claim 8 (m := 0 for the subdivided large squares). For M = k²+1 (unscaled):
  **Σ sᵢ ≤ k + 1/(2k) − (1/2k)Σ m(ksᵢ,θᵢ).**
In particular any packing of k²+1 squares with all sides sᵢ ≤ 1/k and **arbitrary orientations** has Σsᵢ ≤ k + 1/(2k), and each tilted square buys an extra deduction: (1−ksᵢ)²/(2k) if wᵢ ≥ 1, and ksᵢσ(θᵢ)(2−ksᵢ−wᵢ)/(2k) if wᵢ ≤ 1.

*Proof.* (a) *Reduction of large squares:* a square with d ≥ 3/2 is subdivided into m² congruent tilted subsquares of side d/m with m := ⌈d⌉ (a legal refinement of the packing). Since 3/2 ≤ d implies ⌈d⌉ ≤ 2d−1, we have d ≤ m ≤ 2d−1, so each piece has side d/m ≤ 1 ≤ u₁ (condition of Claim 8 holds for pieces), and the bookkeeping (m²−1)/2 − (m−1)d = (m−1)((m+1)/2 − d) ≤ 0 shows the target inequality for the original packing follows from that of the refined packing. So assume all squares satisfy θᵢ=0 or dᵢ ≤ u₁(θᵢ).
(b) *Joint integer pigeonhole:* let G(x,y) := Σᵢ [pᵢ(x)+qᵢ(y)−(Lᵢ−Bᵢ)₊]. G is integer-valued at generic shifts, and by Claims 5,7,8, E[G] = Σᵢ(2wᵢ − E[(Lᵢ−Bᵢ)₊]) ≥ Σᵢ(2dᵢ + mᵢ). If Σ(2dᵢ+mᵢ) > k²+M, then E[G] > k²+M, and since G ≤ Σ(pᵢ+qᵢ) is bounded and integer a.e., the set {G ≥ k²+M+1} has positive measure; intersect with generic shifts to pick (x*,y*). There, by Claims 1–2, Σcᵢ ≥ Σ(pᵢ+qᵢ−1−(Lᵢ−Bᵢ)₊) = G − M ≥ k²+1. But at a generic shift the disjoint-interior squares contain distinct lattice points of Λ inside [0,k]², so Σcᵢ ≤ #(Λ∩[0,k]²) = k². Contradiction. ∎ Confidence: 0.93 (every ingredient proved above; the only delicate points — genericity, integrality, W=w−1 bookkeeping at w=2 — were checked; w=2 occurs only at (d,θ)=(√2,π/4), handled a.e.).

**Claim 10 (Theorem C: exact-BKU limit for small aggregate tilt — the quantitative "η₀" theorem).** Let M = k²+1 squares be packed in [0,k]² (no size restriction), and set Δ := Σᵢ dᵢ² sin2θᵢ (note Δ ≤ 2Σdᵢ²θᵢ ≤ 2k²·max θᵢ). If Δ ≤ 1/4 then
  **Σ dᵢ ≤ k² + 2(k²+2)√Δ,  i.e.  Σsᵢ ≤ k + 2(k²+2)√Δ/k.**
At Δ = 0 this recovers BKU exactly (Σsᵢ ≤ k); it beats the Cauchy–Schwarz bound √(k²+1) whenever √Δ < 1/(8k²)-ish, e.g. when all tilts satisfy θᵢ ≤ 1/(2⁷k⁶)·(k²/Σdᵢ²)… concretely θᵢ ≤ 2⁻⁷k⁻⁶ suffices. This is the strongest statement of the assignment's form "Σsᵢ ≤ k + C·Θ" that I could fully prove, with Θ = √Δ/k·2(k²+2)·k⁻¹·… explicit as displayed.

*Proof.* Suppose Σdᵢ = k² + ε, ε > 0. W_tot := Σwᵢ ≥ k²+ε. A := {x: Σpᵢ(x) ≥ k²+1}: since Σpᵢ is integer with mean W_tot and sup ≤ W_tot+M, |A| ≥ t/(t+M) with t := W_tot−k² ≥ ε; same for B in y with the same W_tot (equal projections — Claim 2's mechanism). By Markov, |{(x,y): ΣLᵢ ≥ 1}| ≤ E[ΣL] = Δ. If |A||B| > Δ, pick a generic (x₀,y₀) ∈ A×B with all Lᵢ = 0; then cᵢ ≥ pᵢ+qᵢ−1 (Claim 1, B ≥ 0), so Σcᵢ ≥ 2(k²+1)−(k²+1) = k²+1 > k², contradiction. Hence (ε/(ε+M))² ≤ |A||B| ≤ Δ, so ε ≤ √Δ·M/(1−√Δ) ≤ 2M√Δ = 2(k²+1)√Δ ≤ 2(k²+2)√Δ. ∎ Confidence: 0.9.

**Claim 11 (honest calibration against Cauchy–Schwarz and the LP barrier).** The unconditional part of Claim 9, Σsᵢ ≤ k+1/(2k), is marginally *weaker* than the trivial bound √(k²+1) = k+1/(2k)−1/(8k³)+…; Claim 9 strictly beats Cauchy–Schwarz only through the deduction term, i.e. when Σᵢm(dᵢ,θᵢ) > k²+1+k²−2k√(k²+1) ≈ 1/(4k²). Both bounds sit essentially at the fractional-LP value ≥ k+1/(2k+1) (Warning 1), which no first-moment argument can cross. The content of Claims 9–10 is therefore: (a) the *integer-lattice mechanism of BKU survives arbitrary tilts* at the LP-optimal level with an exactly computable tilt/size deduction (Claim 9), and (b) it crosses the LP barrier — all the way down to k — in the small-aggregate-tilt regime (Claim 10). By Claim 8 the failure to cross the barrier in general is not sloppiness: the refined per-square budget ties *exactly* at d=1, the conjecture's critical size, for every θ. Confidence: 0.95.

## COUNTEREXAMPLES AND SANITY CHECKS

- **Monte Carlo / deterministic quadrature** (2000² midpoint grids): E[L] = d²sin2θ to ≤ 5·10⁻⁴ at 8 parameter pairs; |p−q| ≤ 1 in 10⁶ samples with no violation; E[min(L,B)] = W² to 5 decimals for d ≤ u₁ (and > W² for d > u₁, as predicted by the E2/E3 patches); margin formulas dσ(2−d−w) / (1−d)² reproduced (e.g. (1.1,0.2): 0.01022 vs 0.01; (0.9,0.2): 0.00973 vs 0.01; residuals = grid error).
- **Coverage Lemma**: direct grid test of "ℓ₁<1 or ℓ₂<1" on [0,W)²: uncovered fraction exactly 0 at (1.0,0.05), (1.0,0.3), (1.1,0.2), (1.15,0.3), (1.35,0.75→mapped).
- **Claim 3 failures found by random search** at all four tested (d,θ), including tilt 0.02 — confirming pointwise failure for tiny tilts.
- **Claim 4**: 2·10⁵ shifts at θ=0.1, d=11.04: zero violations of c ≥ p+q−1.
- **45° diamond** (d=√2−, centered at lattice point): p=q=2, c=1, L=3, B=1: identity c=p+q−1+B−L = 3+1−3 = 1 ✓; and it obeys Claim 8's boundary (d ≤ u₁ = √2, margin (1−d)² ≈ 0.17 — consistent since the diamond's *average* behavior is fine even though this single shift is bad).
- **Exact tilings** (k×k grid, θ=0): Claim 9 gives k² ≤ k²+1/2 with equality-slack ½, consistent; BKU's exact k² is recovered only via Claim 10 at Δ=0.
- **Identities of Claim 6** checked numerically at three (d,θ) pairs to 10⁻⁶.

## DEAD ENDS

1. **Verbatim/pointwise BKU under small tilt (sub-problem 2 as posed):** dead. Claim 3 shows the pointwise inequality fails on a positive-measure shift set for *every* θ>0 in the entire relevant width range w<2; η₀ = 0. Any extension must be statistical.
2. **"Defect measure ~ θ² per (p,q) cell":** false (Claim 5). The corner-avoidance event is a triangle of area Θ(θ), because two of the four triangles are long in one direction. Surplus and defect are the same order; the battle is over constants.
3. **Separate pigeonholes (BKU-style) + Markov on the defect:** the good sets A,B have measure ~ε/M (Warning 5, made precise in Claim 10's proof); requiring E[ΣL]-mass below |A||B| forces Δ ≲ ε²/M², i.e. only the ultra-small-tilt Theorem C survives. Boosting |A| by the tilt surplus S=Σdᵢσᵢ fails: the condition becomes (NΘ̄)³ ≳ N³Θ̄, i.e. Θ̄ ≳ 1.
4. **Recovering the lost ½ via the budget refinement L′=(L−B)₊:** the refinement is *exactly* absorbed at d=1: E[min(L,B)] = W² and (w−1)²−dσ(d+w−2) = (1−d)², so the refined margin vanishes identically at d=1 for every θ (Claim 8). The natural next refinement (p=2,q=1 cells have no budget) cannot help at d=1 either, since the tie is exact. The ½ is the price of spending integrality once (jointly); spending it twice needs product structure that tilts destroy pointwise (Claim 3).
5. **Rotated counting lattices / untilting perturbations:** not attempted beyond the briefing's Warnings 2–3; the boundary excess O(k) resp. rounding cost Σsᵢθᵢ dwarf the "+1" resp. the gap evidence.
6. **Unconditional removal of the size condition in Claim 9:** subdivision (step (a)) handles d ≥ 3/2, and the strip estimate margin ≥ (1−d)² − √2·W·(d−u₁) extends slightly past u₁, but for d ∈ (u₁, 3/2) with small θ the sign is genuinely unresolved. GAP — though by Claim 11 the unconditional statement would anyway be dominated by Cauchy–Schwarz, so this gap is not the bottleneck.

## BEST NEXT STEP

Attack the ½ where Claim 8 says the problem now lives: **second-moment/anti-concentration of the joint counter G.** Claim 9's only lossy step is "E[G] > k²+M ⇒ ∃ shift with G ≥ k²+M+1". If one shows that for near-extremal packings (which by G+V ≤ 1−2ε are near-tilings by near-unit squares) the integer random variable G(x,y) is *not concentrated on a single value* — e.g. Var(G) ≥ v₀ > 0 uniformly, via the explicit sawtooth Fourier expansion of pᵢ(x) (nonzero first Fourier coefficient unless all wᵢ ∈ Z and phases align, which for a genuine near-tiling forces the exact k-grid, already handled by BKU/Claim 10) — then max G ≥ E[G] + c·min(1,Var) and the requirement Σdᵢ > k²+½ drops to Σdᵢ > k²+½−c′. Any uniform improvement of the constant ½ to ½−c′ for *unrestricted* orientations would be the first bound strictly below Cauchy–Schwarz for f(k²+1); and by Singh's monotonicity, pushing the additive constant to o(1) along any subsequence of k proves Erdős's conjecture in full. The concrete first computation: Fourier-expand P(x)=Σpᵢ(x) (each pᵢ is a two-step function with jumps at −Xᵢ^min, −Xᵢ^max mod 1), compute Var(P) = Σ_{m≠0} |Σᵢ ê_m(wᵢ)e^{2πimφᵢ}|²/(2π²m²)-type sums, and characterize the near-extremal configurations with Var(P), Var(Q), Var(ΣL) all ≤ δ — these should be rigid (all wᵢ near integers, phases lattice-aligned, tilts near 0), at which point Claim 10 finishes.