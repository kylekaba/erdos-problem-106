## CLAIMS

**C1. FCMB IS FALSE for every k ≥ 2** (confidence: proved, elementary, numerically verified). Counterexample U_k ("deficient column"): (k−1) columns of k unit squares, plus k+1 squares of side d = k/(k+1) stacked flush-left in the last 1×k column strip. n = k²+1, Σd_i = N exactly, valid AP packing. The gap G is the single strip [k−1+d, k]×[0,k] (width 1/(k+1), height k); it folds k-to-1: π(G) = [d,1)×S¹, |π(G)| = 1/(k+1). Since C(p) = N − #(Λ_p∩G) a.e., **|Av| = k/(k+1) while s = (k+1)(1−d)² = 1/(k+1)**. Violation |Av| − s = (k−1)/(k+1) → 1; ratio |Av|/s = k → ∞. At k=2 it is a 5-square explicit example: 2 units + three 2/3-squares in [1,5/3]×[0,2]; |Av| = 2/3 > 1/3 = s. At k=1 the family degenerates to the split cell (tight) — consistent with "FCMB proved at k=1." No verified theorem of the report is contradicted (budget, structure identity, κ-bound all hold; Σd = N so the conjecture holds with equality).

**C2. New extremal manifold for the conjecture** (proved). U_k(a_1..a_{k+1}), any sides a_j ∈ (0,1] with Σa_j = k in the deficient column: g+s = 1 EXACTLY for every member (2-line algebra: g = k−Σa_j², s = 1−k+Σa_j²). A k-dimensional manifold of equality cases containing split-cells as a face, with gap area up to k/(k+1) — extremals with large positive gap, previously uncatalogued.

**C3. Exact defect formula** (proved). On this family the gap pieces P_j (width 1−a_j, height a_j) satisfy Σ|P_j| = 1−s, and π is injective on each piece, so **|Av| − s = Σ|π(P_j)| − |∪π(P_j)| = folding-overlap loss ≥ 0**: reverse FCMB holds on the whole family; FCMB holds only on the overlap-free stratum (split-cells); any folding overlap is a strict violation, and overlap is generic. Near-injectivity-at-criticality is false: folding is k-to-1 at g+s = 1 exactly.

**C4. The honest corrected form is conjecture-equivalent** (proved identity). g = (1−|Av|) + Σ_{m≥2}|{hits ≥ m}|, so at n = N+1 the conjecture is exactly **OC-FCMB: |Av| ≤ s + Σ_{m≥2}|{p: C(p) ≤ N−m}|**, with equality throughout the deficient-column family (numerically: defect ±0.00000 on equal/uneven/split/staggered members, k=2,3). U_k hides exactly (k−1)/(k+1) of the needed mass at multiplicity m ≥ 2. Every P(hits≥1)-type shortcut is dead; multiplicity-weighted statements collapse to Fubini = the conjecture. GAP: no strictly-weaker true interpolant found; I argue none of this type exists (bounding the overlap loss IS the conjecture).

**C5. Winding fact salvage** (proved, limited). Total y-phase drift along a deficient column = Σ(1−a_j) = 1 (Burgers vector 1); hence ∩_j π(S'_j) = ∅ — no shift captures all k+1 column squares — and the empty-square index on Av is an interval-exchange orbit of winding 1. But winding is 1-D: it spreads square-phases, not gap measure (transverse coordinate can stay locked).

## COUNTEREXAMPLES AND CHECKS
Code: `/private/tmp/claude-501/-Users-kylekabasares-Desktop-erdos-106/6b0597a9-48bd-4e8c-a957-7fc4a05f26d8/scratchpad/session2/F6/refute_fcmb.py`; full derivations in `F6_report.md` same directory. Grid 1200²: k=2 |Av|=0.6667 vs s=0.3333; k=3 |Av|=0.7500 vs s=0.2500 (exact match to prediction). Controls: split-cell a=0.37 exactly tight (0.5338 = 0.5338). Robustness: units shrunk to 0.98 (g+s>1) → 0.6533 > 0.3341; middle square replaced by 0.6-square tilted 0.1 rad → 0.5822 > 0.3822; staggered strip → 0.4444 > 0.3333. Max C = N everywhere (budget respected). Collateral: F4's AP reduction Σ_i|∩_{j≠i}π(S_j)\π(S_i)| ≤ s is false (2/3 vs 1/3 at k=2); the "repetition charges s" lemma is false (k-fold in-phase repetition charged s = 1/(k+1)); the second-moment sufficient condition (E) fails on U_k with Paley–Zygmund tight — the failure is FCMB's own.

## DEAD ENDS
Menu 1 (Kneser/sumsets): no forced structure; folding can be maximally sub-additive under validity — dead. Menu 2 (fault-drift → |π(G)| ≥ 1−s): mechanism real, conclusion refuted; drift spreads gap in one dimension only. Menu 4 (dislocation winding): correct topology, forces an annulus not area. Menu 3: resolved negatively (the headline). All FCMB-sufficient conditions (second-moment, autocorrelation-smallness, ratio forms) are moot.

## BEST NEXT STEP
(1) **Erratum to ERDOS_106_REPORT.md §7A** (I did not edit the shared report): demote FCMB to REFUTED with certificate `scratchpad/session2/F6/{F6_report.md, refute_fcmb.py}`; add the deficient-column extremal manifold (C2) to the catalogue; retire attack order (i)–(iii). Also update memory file erdos-106-attack-state.md. (2) **Retarget F2/F4** at the true enemy shape: on any hypothetical counterexample |Av| ≥ 1−g > s, exactly one square empty per full-capture shift, line-chords give Σd ≤ N + d_{empty(p)} for every p ∈ Av — force either a small ever-empty square or multiplicity mass Σ_{m≥2}|{hits≥m}| ≥ |Av|−s (OC-FCMB) using the fact that multiplicity m needs gap pieces spanning ≥ m−1 vertically (tall thin gaps abut long fault columns whose squares' phases wind — C5 structure). (3) The κ-anti-concentration target (T6) is again the sharpest open sufficient statement; U_k should be added to its tightness test suite.