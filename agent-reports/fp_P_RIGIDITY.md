## CLAIMS

Full proofs: `/Users/kylekabasares/Desktop/erdos-106/agent-reports/session2-derivations/RIGIDITY_derivations.md` (durable copy; also in `.../scratchpad/session2/RIGIDITY/`). Code: `dipstack_check.py`, `lemmaK_check.py`, `enemy_arcs.py` (same two directories). Notation as in ERDOS_106_REPORT.md; additionally t = Σw_i − N, τ = Σd_iσ_i = t − ε, κ = E[(2N+1−P−Q)₊], κ′ = E[(P+Q−2N−1)₊] = κ − 1 + 2t.

**CLAIM 1 (Lemma K — two-variable integer anti-concentration). PROVED, self-contained, elementary. Confidence 0.97.**
If φ, ψ are independent integer-valued random variables with means μ₁, μ₂ ∈ [0,1], then **E[(φ+ψ−1)₊] ≥ μ₁μ₂**, with equality iff both are {0,1}-Bernoulli. Proof from two pointwise integer inequalities — (I) (a+b−1)₊ ≥ a₊1{b≥1} + b₊1{a≥1} − 1{a≥1}1{b≥1}; (II) (a+b−1)₊ ≥ (a−1)₊ + b·1{a≥1} for all integer b — plus a three-case analysis on p = P(φ≥1) vs μ₁, q = P(ψ≥1) vs μ₂, using A = E[φ₊] ≥ max(μ₁, p). The hypothesis μ ≤ 1 is necessary (φ=ψ≡3 gives 5 < 9). Verified: 200,000 random discrete-law pairs, 0 violations. This is the exact quantitative core of BKU's double pigeonhole: it converts "two positive-measure pigeonhole sets must meet" into a mean statement with a *quadratic product gain*.

**CLAIM 2 (Theorem K — the exact "circle Littlewood–Offord" / dip-mass floor). PROVED unconditionally, T6-free. Confidence 0.95. Main positive theorem.**
For every family of N+1 squares in [0,k]² (any sizes, any orientations; packing not even required — only the arc structure p_i = ⌊w_i⌋ + 1_{F_i} and independence of the two shift coordinates): if 0 ≤ t ≤ 1 then **κ′ ≥ t² and κ ≥ (1−t)²**. (Apply Lemma K to φ = P−N, ψ = Q−N, means t; both projections have width w_i, so both axes share t.) For t < 0 the (1−t)² form is genuinely false (jittered split grid: κ = 1.3200 < 1.3456 — the restriction is sharp, not an artifact).
**Tightness: exact equality on every known extremal configuration** — split cell, deficient column U_k, T12 column tilings, deficient-row packing (all t=0, κ=1), and the feasible diamond pair at k=1 (t = 0.3294, κ = (1−t)² to machine precision). This answers F4's Best-Next-Step 3: the true shape of the dip-mass anti-concentration statement is κ ≥ (1−t)₊², no phase data enters, and the assignment's guess "κ ≥ exp(−CS²/Var)-ish" is replaced by a clean polynomial that is exactly attained.

**CLAIM 3 (Resolution of the assigned dichotomy: the κ-branch CANNOT close the conjecture — proved neutralization). Confidence 0.95. Main negative result.**
(a) The naive circle Littlewood–Offord ("endpoint phases spread ⟹ coverage dips below mean−1 on positive measure") is **FALSE**: M arcs of length 1/2 at phases j/M have identically flat coverage M/2 with maximally spread endpoints. Integrality of the mean is the only true forcing, and Theorem K is its exact envelope.
(b) T4 [V] forces every enemy (ε > 1/2−c) to have τ ≥ ε, hence **t ≥ 2ε > 1−2c**, hence the floor (1−t)² < 4c² ≪ 2c. Moreover κ = 0 is exactly attainable at the arc level (all w_i ≥ 1 gives P,Q ≥ N+1 everywhere). Measured (enemy_arcs.py, t = 1/2): min κ over all phase strategies = 0.25 = (1−t)², attained by *equidistributed* phases; *clustering raises* κ (column phases: κ ≈ 0.9). **Conclusion: phase spread costs the enemy nothing through dip mass; no measure-theoretic lower bound on κ from phase data can reach 2c. The dichotomy "W ∨ Littlewood–Offord-κ" has a structurally dead second jaw — the second jaw must be geometric (C″ unions / α′ gap), not measure-theoretic.** This is the κ-side analogue of F4's No-Repair theorem.

**CLAIM 4 (Dip structure theory D1–D4 + (★★) + forced deficit clustering). PROVED. Confidence 0.93.**
Per axis, with U = {w_i<1} (deficit arcs J_i, |J_i| = 1−w_i, endpoints = the bbox-extreme phases), Ǧ = Σ_U 1_{J_i} (simultaneous-miss count), F₊ = oversize frac-hit count, r = 1 + Σ(⌊w_i⌋−1)₊:
- **D1 (exact dip identity):** {P ≤ N} = {Ǧ − F₊ ≥ r} a.e.
- **D2:** E[Ǧ − F₊] = r − t.
- **D3 (stack forcing):** ∫_{E_x} Ǧ ≥ 1−t and (1−t)₊ ≤ H_x·δ_x, where E_x = dip set, δ_x = |E_x|, H_x = max stacking of deficit arcs over E_x.
- **D4:** κ ≥ δ_xδ_y; **κ ≥ (1−t)₊·max(δ_x, δ_y)**; hence κ ≥ (1−t)₊²/min(H_x,H_y).
- **(★★):** for t ≤ 1, unconditionally, 1 − ε ≤ τ + √(κ·min(H_x,H_y)); equality on deficient column, split cell, T12, deficient row.
- **Corollary R (the strongest true variant of the mod-1 Rigidity Lemma I could prove):** if ε > 1/2−c, t < 1, and κ + Σm_i < 2c (else dead by T6), then **on both axes** ≥ (1−t) of deficit-arc mass mod 1 is crammed into ≤ N+1 arcs of total measure < 2c/(1−t), with ≥ (1−t)²/(2c) undersize squares simultaneously trapped strictly inside the unit strips of one common lattice (at c=1/400, τ ≤ 1/4: mass ≥ 1/5 in measure ≤ 0.025, stacking ≥ 8, per axis). So clustering is *forced*, not hypothesized — the payment currency is κ, exactly as T6 demands. **Honest limitation: by Claim 3(b) the true enemy sits at t ≥ 2ε where this is vacuous.** Lemma S (AP stack geometry): a strip holding k+1 trapped squares carries deficit ≥ 1; the deficient column is the minimal saturator.

**CLAIM 5 (Theorem K′ — tilt-mass window; contains BKU). PROVED modulo T6's chain [P]. Confidence 0.85.**
Via MPI ⟹ E[D] ≥ κ′ ≥ t₊² (Lemma K) and the verified per-square margins: for t ≤ 1, **(ε+τ)₊² ≤ 2τ − Σm_i**; for t ≥ 1, 2ε ≤ 1 − Σm_i. At τ = 0 this forces ε ≤ 0: a third proof shape for BKU's endpoint (mean-form with quadratic gain, no max-pigeonhole). Quadratic window τ ∈ [(1−ε)−√(1−2ε), (1−ε)+√(1−2ε)] for t ≤ 1; exactly tight (0 ≤ 0) on the entire t=0 extremal manifold. Honest: at ε → 1/2 its lower root is weaker than T4; its value is unification and the t ≤ 1 upper pinch τ ≤ 1−ε.

**CLAIM 6 (The consolidated open core, restated as a proved covering law). Confidence 1.0 in the meta-statement.**
Every enemy must satisfy simultaneously: (i) t ≥ 2ε > 1−2c (T4): one full unit of width faked by tilt; (ii) by D1+D4(a), on at least one axis, Ǧ − F₊ ≤ r−1 pointwise outside dip sets with δ_xδ_y < 2c — undersize misses pointwise dominated by oversize frac-hits, an exact integer covering system paid unit-by-unit from width surplus; (iii) C″ [V]: Σ(1−w)₊ + max(U_x,U_y) ≥ 1 with Σ(1−w)₊ = Σ(w−1)₊ + (1−t); (iv) α′/α′-edge gap costs at exposed interfaces. The phase degree of freedom is now fully mapped: it is free in κ (Claim 3) and constrained only through the *defect-phase unions* U of C″ and gap geometry.

## CHECKS

- Lemma K: 200k random integer-law pairs (support −3..5, means in [0,1]): 0 violations; earlier unrestricted run *correctly* found 20,906 violations for means > 1, confirming sharpness of the μ ≤ 1 hypothesis.
- Arc engine is exact (piecewise-constant breakpoint arithmetic, no sampling). 4000 random arc systems (w ∈ (0.05,2.4), including r ≥ 2 cases): D1 pointwise identity, D2, D3, forcing — 0 failures; worst Hδ/(1−t) = 1.0000 (equality realized, bound sharp).
- 10 machine-verified packings (SAT-disjointness + containment): deficient columns k=3,4 (+tilted t=0.02,0.03), split cell, T12 column tiling k=4, deficient-row, tilted 2×2 grain, jittered split grid, diamond pair. All of D1–D4, (★★), K, K′, T6 pass; equalities exactly where predicted (κ = (1−t)² on all extremals *and* the diamond pair; Hδ = 1−t on the extremal manifold).
- Enemy-shaped arc systems (t = 1/2, M = 17, 5 phase strategies × 3 spreads × 60 trials): min κ = 0.2500 = (1−t)² (equidistributed); clustered/column κ = 0.79–0.99; worst κ·minH/(1−t)² = 1.0000. Confirms both Theorem K's sharpness and Claim 3's direction (clustering raises κ).
- Consistency: running-bond packings tolerated correctly (spread phases + large κ + ε ≤ 0: no tension); deficient column tight in (★★) through its *winding* axis (min H = 1) — matches F6's winding salvage exactly.

## DEAD ENDS

1. **Naive circle Littlewood–Offord** (dip mass from endpoint spread) — false; equidistributed arcs are flat. Retire.
2. **The κ-half of the assigned dichotomy as a conjecture-closer** — structurally dead (Claim 3): T4's forced width surplus puts the enemy where κ ≥ (1−t)² is vacuous, and κ = 0 arc systems exist. Any future κ ≥ 2c claim must inject geometry (disjointness beyond the budget), not phases.
3. **K′ as an enemy constraint at ε → 1/2** — subsumed by T4/T6; keep it as the unifying envelope and BKU generalization only.
4. **Deriving PH(K,λ) of Theorem W from my clustering Corollary R** — the forced clustering concerns *deficit* arcs of undersize squares in the t < 1 regime; W needs all bbox-extreme phases (including oversize/tilt-defect ones) in the t ≈ 1 regime. Bridge not available; the original mod-1 Rigidity Lemma (report §7B) **remains open**, and per Claim 3 its "pays gap ≥ 2c" clause cannot be replaced by any dip-mass payment.
5. ℓ¹/L² accumulation loss (Route B Dead End 5): unchallenged; I did not attempt to reprove it and nothing here repairs it.

## BEST NEXT STEP

Attack the **enemy covering law** (Claim 6(ii)) — the proved successor of the rigidity target, and capture-side as F4's No-Repair demands. Concretely: on the dominated axis, each phase point needs Ǧ(x) ≤ F₊(x) + r − 1, where every unit of F₊ is an arc of length w_i − 1 ≤ d_iσ_i + (d_i−1)₊u₁ pinned at square i's bbox-extreme phases. Combine with C″'s disjunctive form [V] (|A∖∪B^x|·|B∖∪B^y| = 0): the oversize squares that pay the covering on the x-axis must simultaneously keep their tall-triangle phase sets from being charged by U_x. Prove: *a square cannot both dominate a miss (w ≥ 1, frac-hit at phase x) and have its corner-triangle set avoid x* — i.e., quantify the overlap of F_i (oversize frac arc) with B_i^x (its own tall-triangle set): both live at the bbox extremes and have comparable lengths (w−1 vs d sinθ, and w−1 ≤ dσ ≤ d sinθ + d(cosθ−1) ≤ d sinθ). If F_i ⊆ B_i^x up to O(θ²) (check: the frac arc of an oversize tilted near-unit square sits exactly where its tall triangles sit), then pointwise covering forces U_x ≥ measure covered by F₊-arcs ≈ Ǧ-mass, feeding C″ with a *derived* U lower bound — the two verified weapons close a loop that no longer has a free phase parameter. This is a finite, checkable geometric statement (per-square arc containment) and I flag it as the highest-value unproved lemma left by this assignment.