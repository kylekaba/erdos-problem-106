## CLAIMS

Full derivations: `/Users/kylekabasares/Desktop/erdos-106/agent-reports/session3-derivations/G2_MASTER_ASSEMBLY.md` (durable; also in `.../scratchpad/session3/G2/`). Machine checks: `G2_g2_checks.py` (same directory), 14 groups, ALL PASS. Notation as in ERDOS_106_REPORT.md; Enemy(c) := packing of N+1 squares in [0,k]² with eps > 1/2 − c. Note: sibling verifier G1 (G1_VERIFICATION.md, same directory) this session promoted Lemma 0, A″, S2 (incl. the ledger), S1/S3/S4, Lemma K, Theorem K to [V]; my independent re-derivations of Lemma 0/A″/S2-ledger in Part I are a third pass. So everything below except T6 and the two named Lemmas rests on [V] pieces.

**CLAIM 1 (The load-bearing computation — the assignment's grind, exact; PROVED, components [V]).** For any packing with eps > 0 and b0 < eps: beta = 1 − eps + b0 − Γ < 1 (identity, Γ ≥ 0 pointwise; machine I3), hence S2 applies, and the exact chain 1−g ≤ |Av| ≤ Σ_short(1−w)² + U_x+U_y+V_x+V_y with 1−g = s+2eps and the bracket identity **s − Σ_short(1−w)₊² = Σ_{w<1} d σ(2−d−w) + Σ_{w≥1}(1−d)² ≥ 0** (from (1−w)² = (1−d)² − dσ(2−d−w), exact for all d,θ; machine I1/I2) yields the exact requirement:
**U_x+U_y+V_x+V_y ≥ 2eps + Σ_{w<1} m_i + Σ_{w≥1}(1−d_i)².**
Answer to the assignment: the bracket loses nothing — longs pay their full (1−d)², shorts their T6 margin, with NO T6 input (pure algebra). Case III does NOT "close now": by the mass fact (Claim 4) the requirement is always satisfiable in raw mass; what closes unconditionally is only: literal whisper t0 < eps/(N+eps) (T4, machine I11), AP (BKU; b0<eps case BKU-free via S4), and — new — saturating enemies at small k (Claim 3).

**CLAIM 2 (Theorem S3+ — the vernier dichotomy; NEW assembled theorem, all components [V]; confidence 0.95).** Call an x-phase *saturating* if it is good (∉ ∪B_i^x) and all p_i(x) ≥ 1. Every packing of N+1 squares with eps > 0 satisfies at least one of:
**(I) [vernier]** some axis has positive-measure saturating phases; a.e. such phase witnesses an over-full lattice line: k+1 squares with pairwise interior-disjoint chords, each exactly d_j sec θ_j, summing ≤ k, line chord-deficit ≥ 1; hence β ≥ 1 and b0 ≥ eps + Γ;
**(II) [union]** U_x+U_y+V_x+V_y ≥ 2eps + margins (Claim 1's RHS).
This strictly upgrades S3: branch (i) is promoted from arithmetic (b0 ≥ eps+Γ) to geometric vernier structure, because S2's proof needs only non-saturation, not β < 1 — β < 1 was merely a sufficient condition. Killing branch (I) is therefore strictly easier than killing "b0 ≥ eps": a top-heavy but non-saturating enemy falls to (II).

**CLAIM 3 (Small-k saturation kill; NEW, unconditional).** A saturating Enemy(c) has s ≥ 1/(k+1) (Cauchy–Schwarz on the vernier line's deficits, valid with negative terms; machine I7), so **every Enemy(c) with k ≤ 1/(2c) − 1 is in branch (II)**. At c = 10⁻⁵: all k ≤ 49,999. Lemma L1 below is needed only beyond that.

**CLAIM 4 (Mass never obstructs — the preamble's strategic fact, formalized; PROVED).** Raw arc mass R = 4Σd sinθ + 2Σ(w−1)₊ ≥ 4Σdσ ≥ 4eps whenever eps > 0 (sinθ ≥ σ pointwise = 1−cosθ ≥ 0; T4 chain eps ≤ Σdσ/u1 ≤ Σdσ; machine I4/I6). So branch (II) can never fail for lack of mass; every kill of (II) must be a rigidity statement about union collapse. Also re-derived: t ≥ 2eps (t = eps + τ, τ ≥ eps by T4) — the fp_P_RIGIDITY C3b claim is confirmed.

**CLAIM 5 (Conditional Master Theorem; the deliverable).** Define:
> **Lemma L1 (vernier kill; UNPROVED).** ∃ absolute c1, K1: no packing with k ≥ K1, eps > 1/2 − c1 has a positive-measure saturating axis. *(Weakest sufficient form; strictly weaker than "no enemy has b0 ≥ eps". Unconditionally, its enemy already must have s ≥ 1/(k+1), b0 ≥ eps+Γ ≈ 1/2, ≥ (1/2−c)/√(2c) oversize squares, k > 1/(2c)−1, and contain a deficient-column-like vernier of k+1 squares with deficits ~1/(k+1). Why true: every known vernier realization pays gap ~ k(1−width) ~ 1 ≫ 2c unless flanked by encroaching tilted oversize squares, which the s < 2c budget fights.)*
> **Lemma L2 (union bound = final weakened mod-1 Rigidity Lemma; UNPROVED).** ∃ absolute c2, K2: every packing with k ≥ K2, eps > 1/2 − c2 has U_x+U_y+V_x+V_y < 1 − 2c2. *(Pure union-measure statement: no arc counts, no cover structure, no b0 hypothesis.)*

**Theorem (conditional).** Assume L1, L2. Then with c0 = min(c1,c2): **f(k²+1) ≤ k + (1/2 − c0)/k for EVERY k ≥ 1.** Proof: for k ≥ K0 = max(K1,K2), an enemy satisfies S3+ (I) or (II); (I) contradicts L1, (II) gives U+V > 1−2c0 contradicting L2; for k < K0, Singh's published monotonicity (k·eps(k) non-decreasing) transfers the bound down. Dependencies: Props 1.1–1.8/S3+ ([V] pieces only) + Singh. Nothing else — in particular no T6, no Theorem W, no α′ enter the minimal assembly; they enter only the *proof program for L2*.

**CLAIM 6 (Minimality; PROVED as meta-statements).** Two lemmas is minimal: L1 cannot be dropped (branch-(I) enemies have no union obligation; the deficient column sits exactly on the saturation boundary at eps = 0 and nothing verified moves against its eps ≈ 1/2 hypothetical); L2 cannot be dropped (Claim 4 shows (II) is mass-satisfiable; No-Repair [V] and RIGIDITY Claim 3 [V] prove no measure-theoretic substitute exists). The granular bridges merge: X1 (steep exposure) + X2 (whisper dislocation cost) ⟹ L2; X3 = L1 stays separate. There is no crack at b0 ∈ [eps, eps+Γ): saturation is handled by L1 regardless of b0, non-saturation by (II).

**CLAIM 7 (Coherence trichotomy, formalized, with kill chains and constants — the proof program for L2; Part II–III of the file).** Definitions: whisper threshold t0 = 1/(4k²); steep = θ > 2t0; η-interfaces (η = 1/4) with mismatch functional I = Σ L_ij·min(tan α_ij, 1/3) (walls as tilt-0 squares); grains = η-connected orientation-coherent components. Split the union U+V = M_steep + M_whisper by which class carries it (NOT by which carries T4 mass — an earlier error I caught: at t0 = 1/(4k²) whisper squares can just barely carry T4).
- **Case I (steep-carried):** charge ratio (union contribution)/(α′-edge charge) ≤ sup_θ 4(1+√2c)sinθ·240/min(tanθ/2, 1/3) = **2078** (machine I8, sup at π/4); with overlap multiplicity C_ov = 12: M_steep ≤ ~25,000·G. Bridge = **LEMMA X1** (exposure bookkeeping = Gap A quantified): distinct-phase steep squares cannot simultaneously dodge α′-edge (1/240), the gradient corollary (1/120, row-count-free), and the Wall lemma. Its enemy: a coherent 45° grain (internal phases scatter at 0.414/step with interfaces hidden); survives at 23× margin at c = 10⁻⁵ (machine I13) but the bookkeeping is unproved.
- **Case II (whisper, clustered):** fully proved given cluster data — W's fattening step [V]: K arcs of length λ give M_whisper ≤ 2λ + 2.2K/k²: clustering *destroys* the union mass (II) demands. Self-killing; no new lemma.
- **Case III (whisper, scattered):** needs ~k²/2 distinct phases. Variance does NOT forbid this (ℓ¹ accumulation √(2ck) ≫ 1 for k > 1/(2c) — Route B Dead End 5 confirmed unrepaired). Bridge = **LEMMA X2** (dislocation cost): M_whisper ≤ C_X2(√c + 1/k) for G ≤ 2c packings; the deficient column's exchange rate (gap ~1 per unit winding) must be proved cheapest-possible. This is the single deepest open point; its enemy is the gentle-giant multigrain with grain-boundary phase offsets.

**CLAIM 8 (Explicit c0).** Instantiated arithmetic: 2·26,400·c + 30(√c + 1/k) < 1 − 2c closes at **c0 = 10⁻⁵, K0 = 300, margin +0.277** (machine I9; +0.05 at c = 1.4·10⁻⁵). Headline: modulo X1 (C_ov ≤ 12), X2 (C_X2 ≤ 30), L1: f(k²+1) ≤ k + (1/2 − 10⁻⁵)/k for all k. Bottleneck: α′-edge's 1/240 × overlap multiplicity taxing a single 45° square's 0.7-length arcs (this alone caps any c0 from this route at ~2·10⁻⁴).

**CLAIM 9 (Structure-of-Counterexamples Theorem U — best unconditional statement; PROVED).** Every packing with eps > 1/2 − c (c ≤ 1/12) satisfies ALL of (Part V, Tier 1, [V]/published only): (1) g+s = 1−2eps < 2c, all sides in 1±√(2c); (2) Σdθ ≥ Σd sinθ ≥ τ ≥ eps and t ≥ 2eps: a faked unit of projection width; (3) not AP, k ≥ 2; (4) no common Pythagorean frame with c|k; (5) Δ > ((1−2c)/(4(N+1)))²; (6) max(U_x,U_y) ≥ eps − b0: oversize mass ≥ 1/4 or a corner-triangle phase union ≥ 1/4 − c; (7) Theorem W's clustering prohibition for whisper enemies with b0 ≤ 1/4; (8) every eligible exposed interface has min(tanα − tanφ0, 1/3) < 480c; (9) crystallization: mismatch cannot be buried, sector gaps ≤ 2c budget; (10) wall discipline ≤ 2c; (11) min-tilt < 1/(0.2745k − 348) for k ≥ 1274; (12) upward propagation (Singh); (13) **S3+ dichotomy: vernier (with s ≥ 1/(k+1), b0 ≥ eps+Γ, k > 1/(2c)−1) or union ≥ 2eps + margins — now Tier 1 via G1's promotions**; (14) Theorem K floors vacuous by design (t ≥ 2eps). Tier 2 (adds T6 [P]): κ + Σm_i ≤ 2c; 2eps ≤ |Av| + U_x + U_y − Σm_i; the D1 covering law.

## CHECKS

- `G2_g2_checks.py`: 14 groups ALL PASS — identity (1−w)² = (1−d)² − dσ(2−d−w) to 1.8e-15 (200k random (d,θ), full range d ∈ [0.05, 2.5]); bracket identity on 200 random populations; β = 1−eps+b0−Γ and Γ ≥ 0 pointwise; σ ≤ sinθ ≤ θ ≤ 1.1108 sinθ; (1−d secθ)₊ ≤ (1−d)₊; (w−1)₊ ≤ (d−1)₊+dσ; structure identity + S3(II) rearrangement end-to-end (500 populations); vernier Cauchy–Schwarz with negative deficits (2000 trials); steep charge ratio sup = 2077.2 (numeric scan, 400k points); c0 fixed-point margins printed; whisper/T4 thresholds; W phantom arithmetic (0.36534 < 0.4975) reproduced; 45°-single-square attack margin 23×; w_max = 1.445 < 2 (V-arcs stay arcs).
- Packing-level numerics were not re-run here (done by SQUEEZE/RIGIDITY and independently by G1: ~10⁶ shifts, 7 SAT-validated packings, 0 violations); my contribution is the third analytic pass on Lemma 0/A″/S2-ledger (Part I, full write-outs) — all correct.
- Adversarial self-attack (Part VII): big-square ledger range, saturation a.e. logic, negative vernier deficits, the 45° single-square attack, whisper-mass double-count (caught and fixed in D1), W's all-tilts hypothesis (only its fattening *step* is used per-class), margin signs. No cracks found; the b0 ∈ [eps, eps+Γ) seam is covered.

## DEAD ENDS

1. **"Case III closes now" — no.** The b0-light chain is exact but terminates in a satisfiable requirement (Claim 1/4); only T4-whisper, AP, and small-k-saturating die outright. The assignment's optimism was right about the algebra, wrong about the contradiction.
2. **Raw-mass kills of S3(ii)** — permanently dead (Claim 4): any future kill must target union collapse.
3. **Merging L1 into L2** — impossible: branch-(I) enemies owe no union.
4. **Replacing L2 by a PH(K,λ) hypothesis** — strictly stronger assumption; union form is the weakest S3+ accepts. Conversely replacing L2 by κ/variance/gap-mass routes — dead by No-Repair + RIGIDITY Claim 3 (both now [V]).
5. **c0 > ~2·10⁻⁴ via this architecture** — blocked by the 45° single-square attack on α′-edge's constant; pushing c0 → 1/2 (full conjecture via Singh) needs a qualitatively sharper interface tax, not better bookkeeping.

## BEST NEXT STEP

Attack **Lemma L1 via the vernier geometry**, which S3+ has now made concrete and which is the *less* deep of the two lemmas: a saturating Enemy(c) contains, at positive x-measure, k+1 squares whose vertical chords tile a lattice line with total slack ≤ k − Σd secθ and per-square deficits ~1/(k+1), inside a width-~1 strip. Prove: such a vernier strip forces gap ≥ γ·(1 − O(k t0-encroachment)) with γ absolute — the deficient column realizes γ = k/(k+1) and tilted flanking squares can encroach only O(θ²) area per neighbor (Lemma 0's middle-region geometry limits how far a whisper-tilted neighbor can penetrate the strip). If L1 lands, the entire conjecture-with-margin reduces to the single union statement L2, whose proof program (trichotomy, Claim 7) is fully scaffolded with only X1 (finite geometric bookkeeping, α′-edge + gradient + Wall) and X2 (dislocation cost — the one deep gap) outstanding. Files: `agent-reports/session3-derivations/G2_MASTER_ASSEMBLY.md`, `G2_g2_checks.py`; update ERDOS_106_REPORT.md §7C to record S3+ (vernier upgrade), Cor 1.9 (small-k saturation kill), the two-lemma minimal set, and G1's [V]-promotions.