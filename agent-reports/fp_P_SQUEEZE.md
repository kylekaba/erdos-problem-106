## CLAIMS

All proofs in full: `/private/tmp/claude-501/-Users-kylekabasares-Desktop-erdos-106/6b0597a9-48bd-4e8c-a957-7fc4a05f26d8/scratchpad/session2/capture_squeeze/DERIVATIONS.md`; verification code `squeeze_check.py` (same directory, all assertions pass). Notation as in ERDOS_106_REPORT.md. New objects: **β := Σᵢ(1 − dᵢ secθᵢ)₊** (chord-deficit mass), **V_x := |∪ᵢ{x : pᵢ(x) ≥ 2}|** (multi-line arc union; for wᵢ ∈ [1,2) an arc of length wᵢ−1 at the bbox-min phase), V_y likewise; U_x, U_y are exactly C″'s tall/wide triangle phase-arc unions.

**CLAIM 0 (sanity check requested by the assignment — confirmed).** The AP (d ≤ 1) squeeze sketched in the assignment is verbatim F1's restricted FCMB-AP corollary: Lemma A gives α₀ = β₀ = 0 on an enemy (σ = 1−ε < 1), the C3 decomposition collapses to Σαᵢβᵢ ≤ s, and |Av| ≥ 1−g > s kills it. Nothing new there; everything below is the tilted extension.

**CLAIM 1 (Lemma 0 — chord constancy; new exact geometric fact). PROVED.** The vertical chord of a square of side d, folded tilt θ ∈ (0,π/4], is ≤ d secθ everywhere and **exactly d secθ identically on the middle region [d sinθ, d cosθ] of its bbox x-range** (bottom envelope = edge AB, top = edge DC, both slope tanθ; difference = d cosθ + d sinθ tanθ = d secθ). Horizontal chords obey the same law by the square's 90° symmetry. Hence: **if x mod 1 ∉ ∪ᵢBᵢˣ, every lattice line meets every square it touches in the middle region, with chord exactly dᵢsecθᵢ.** This is what replaces "chord = side" for AP squares and revives the dead pigeonhole. Confidence 0.97 (elementary; machine-checked to 1.6e-15, 24,000 samples).

**CLAIM 2 (Lemma A″ — tilted over-full-line exclusion; generalizes F1's Lemma A two ways). PROVED.** For any packing of N+1 squares in [0,k]² with **β < 1**: for a.e. x with x mod 1 ∉ ∪ᵢBᵢˣ, some pᵢ(x) = 0; symmetrically in y. *Proof:* generic good x with all pᵢ ≥ 1 gives ≥ N+1 incidences on k lines; some line carries ⌈(N+1)/k⌉ = k+1 squares; by Lemma 0 their chords are exactly dⱼsecθⱼ, interior-disjoint in [0,k], so Σ_L dⱼsecθⱼ ≤ k and Σ_L(1−dⱼsecθⱼ) ≥ 1, forcing β ≥ 1. ∎ Two generalizations of Lemma A: tilts allowed, **and big squares allowed** (positive-part deficits; at θ ≡ 0 the hypothesis Σ(1−dᵢ)₊ < 1 replaces "all d ≤ 1, Σd > N"). Tilts *help* the hypothesis: (1−d secθ)₊ ≤ (1−d)₊ with quadratic-in-θ gain. **Sharpness: the constant 1 is exact** — the deficient column has β = 1 exactly with good full-hit measure k/(k+1) > 0 (and split cell: β = 1, full-hit measure min(a,b) on one axis). Confidence 0.95.

**CLAIM 3 (Theorem S2 — tilted capture-measure bound; the main new theorem). PROVED.** Every packing of n = N+1 squares (any sizes, any orientations) with β < 1 satisfies
**|Av| ≤ Σᵢ(1−wᵢ)₊² + U_x + U_y + V_x + V_y.**
*Proof skeleton:* off the bad phases (cost U_x+U_y), cᵢ = pᵢqᵢ (verified corner-triangle product structure). On Av∩E: Σpᵢqᵢ = N; idles (cᵢ=0) force wᵢ < 1; multi-captures (cᵢ≥2) force wᵢ ≥ 1; idle count = 1 + (multiplicity excess). By Lemma A″ both M_X(x), M_Y(y) are nonempty, so: exactly-one-idle events are contained in the disjoint products {pᵢ=0}×{qᵢ=0} (total ≤ Σ(1−wᵢ)₊²), and ≥2-idle events require a multi-capture, i.e. x ∈ V_x-union or y ∈ V_y-union. ∎ **The hypothesis β < 1 is razor-sharp**: the deficient column (β = 1) violates the conclusion by (k−1)/(k+1); the whole extremal manifold (columns, stack gadgets, split cells) sits at β = 1 identically. AP corollary: any AP packing with Σ(1−dᵢ)₊ < 1 has |Av| ≤ Σ_{d<1}(1−dᵢ)² + V_x + V_y — restricted FCMB-AP without the d ≤ 1 restriction. Confidence 0.9.

**CLAIM 4 (Corollary S3 — the assembled enemy squeeze). PROVED.** Every packing of N+1 squares with ε = Σdᵢ − N > 0 satisfies at least one of:
- **(i)** b₀ := Σ(dᵢ−1)₊ ≥ ε + Γ, where Γ := Σᵢ[(1−dᵢ)₊ − (1−dᵢsecθᵢ)₊] ≥ 0 is the chord-gain of the tilts (this is exactly β ≥ 1, via Σ(1−d)₊ = 1−ε+b₀);
- **(ii)** U_x + U_y + V_x + V_y ≥ 2ε + Σ_{wᵢ<1} mᵢ + Σ_{wᵢ≥1}(1−dᵢ)², with mᵢ = dᵢσᵢ(2−dᵢ−wᵢ) the exact T6 margins (chain: 1−g ≤ |Av| ≤ S2-bound; 1−g = s+2ε; exact identity (1−w)² = (1−d)² − dσ(2−d−w)).

Special cases, all proved: **S1 (all-short, hypothesis-free):** all wᵢ < 1 and Σd > N ⟹ U_x + U_y ≥ 2ε + Σmᵢ (β < 1 and b₀ = 0 automatic; V = 0). **S4 (BKU-free AP kill up to b₀):** no AP packing has Σd > N and b₀ < ε — for AP, V_x + V_y ≤ 2b₀ < 2ε contradicts (ii); extends F1's measure-theoretic BKU reproof from {all d ≤ 1} to {b₀ < ε}, still without using BKU. Confidence 0.9.

**CLAIM 5 (κ-coupling — the assignment's mechanism, made precise). PROVED (unconditional; feeds through T6 which is [P]).** κ ≥ (1 − U_x − U_y − |Av|)₊ always (on {ΣLᵢ=0} ⊇ E: P+Q ≤ 2N+1 pointwise via C ≤ N; on top of {hits ≥ 1}: P+Q ≤ 2N, integrand ≥ 1). With T6: **2ε ≤ |Av| + U_x + U_y − Σmᵢ**, equivalently 2ε + Σmᵢ + |π(G)| ≤ 1 + U_x + U_y, with no β hypothesis. The speculative inequality in the assignment ("g + 2Σ(1−w)₊ + U_x + U_y ≥ 1") is indeed derivable but strictly weaker than both C″ and this; retired.

**CLAIM 6 (test-configuration audit — assignment items (i)–(iv)).** (i) AP enemy d ≤ 1: (i) fails, (ii) reads 0 ≥ 2ε + Σm > 0 — contradiction; reproves BKU (d≤1) by exactly F1's mechanism ✓. (ii) Deficient column: ε = 0, theorem correctly silent; it sits exactly on the boundary of alternative (i) (b₀ = 0 = ε + Γ) and violates S2's conclusion — consistency and sharpness both confirmed numerically ✓. (iii) Coherent whisper grid: b₀ = 0 kills (i); U and V are unions of K-clustered arcs, so (ii) forces 2ε ≤ 2λ + O(K·d_max·t₀): Theorem W's kill with the per-square penalty Σ(d−1)₊ replaced by the **union** V_x (strictly better on coherent configs) and no PH hypothesis inside the theorem ✓. (iv) 45°: β ≈ 0 but U_x = U_y = 1 — (ii) vacuous; 45° remains α′/T2 territory (expected: B-arcs saturate) ✓.

## CHECKS

Code: `squeeze_check.py` (12 SAT-validated packings, containment + pairwise separating-axis checked; grids up to 420² with generic irrational offsets; 200,001-point arc rasterization).
- **Chord constancy:** max |chord − d secθ| = 1.55e-15 in middle region; 0 exceedances of d secθ (300 squares × 80 chords).
- **Lemma A″ mechanism:** 531/531 sampled good full-hit x across 4 column configs (AP k=2,3; tilted t=0.02, 0.05) produced an over-full line with k+1 squares, disjoint chords summing ≤ k, each chord = d secθ to 1e-9, line deficit ≥ 1. Across all 12 packings: good full-hit measure > 0 occurred only with β ≥ 1 (columns: β = 1.0000 exactly, gfh = k/(k+1); tilted columns β = 1.0384, 1.0905; split cell β = 1, gfh_y = 0.3700 = a; random strips β = 1.24–1.50); both β < 1 packings (10-diamond 45° family, β = 0.1005) had gfh = 0.
- **E-structure:** zero violations of cᵢ = pᵢqᵢ on E, zero idle longs, zero multi-capturing shorts, over ~2.1M grid shifts including a long tilted square (w = 1.147, V_x = 0.1496 = frac(w) ✓) and a long AP square (d = 1.3, V_x = 0.3000 ✓).
- **Budget:** max C = N on every config. **S2:** satisfied on every β < 1 config; **sharpness:** col_k2: β = 1.000000, |Av| = 0.6667 > RHS = 0.3333. **Algebra identity** (1−w)² = (1−d)² − dσ(2−d−w): 2.2e-16 over 10⁴ random (d,θ). Structure identity g+s and ε cross-checked per config (e.g. bigAP: Σd = N, g+s = 1.0000, |Av| = 0.5200 = s exactly — a new tightness instance of the AP-extended S2 at β = 1.3 where only the weaker C3-with-cross-terms form is claimed).
- **Adversarial self-attack (what I tried to break):** (a) θ = π/4 degeneracy — middle region is a point; B-arcs cover the projection; lemma vacuous, not wrong ✓. (b) chord interiors vs. square interiors: tangency/vertex coincidences are null in x; genericity handles them ✓. (c) pigeonhole with multi-incidence squares (w ≥ 1): incidences only increase; a square meets a given line once ✓. (d) longs with d secθ > 1 on the over-full line: contribute negative deficit terms, strengthening the contradiction ✓. (e) attempted real-packing counterexample to S2 with β < 1 and U < 1: appears geometrically impossible (β < 1 needs chord mass > N, forcing tilts ≥ ~30° on most squares, whose B-arcs then push U → 1 unless phases coincide, which tilted-grid phase arithmetic (0.587i − 0.492j mod 1) prevents) — consistent with S2's role being enemy-side, like restricted FCMB-AP whose hypothesis is also unsatisfiable by real packings.

## DEAD ENDS

1. **Line-chord Σ′d ≤ N on Av for tilted squares (the assignment's opening route) is dead as stated** — chords taper to 0 near projection edges, so no per-capturer side bound exists pointwise; the salvage is not the capture-side chord sum but the *full-hit-side* chord sum (Lemma A″), which only needs chords on ONE over-full line, where the good-phase restriction pins them to d secθ.
2. **"Exactly one idle" is unrecoverable for w ≥ 1**; the correct replacement is the idle/multiplicity ledger |I| = 1 + μ plus the observation that μ ≥ 1 is an arc-union event (V), not a per-square Markov event. Bounding T_{≥2} by Markov (2Σ(w−1)₊) instead of the union V loses the whisper regime entirely — the union form is load-bearing.
3. **The assignment's inequality g + 2Σ(1−w)₊ + U_x + U_y ≥ 1** — derivable but strictly dominated by C″; dropped.
4. **45° regime:** no counting statement of this family can bite (B-arcs of length d sinθ ≈ 0.7d per square saturate U); confirmed vacuous, left to the α′/T2 jaw.
5. **Dropping β < 1 via cross-terms:** at β = 1 the full decomposition acquires α₀-type full-hit terms realized by the column (α₀ = k/(k+1)); any β ≥ 1 extension must carry them explicitly — no free repair (this is the capture-side shadow of the No-Repair theorem).

## BEST NEXT STEP

The enemy surviving S3 must have **(a) b₀ ≥ ε + Γ (top-heavy sides) or (b) unclustered mod-1 phases making U+V ≈ Θ(1) out of o(1)-length arcs** — and (b) is verbatim the mod-1 Rigidity Lemma target already named in §7B. Two concrete advances now possible: **(1)** the rigidity target can be weakened: Theorem S3 needs only a bound on the *unions* U_x+U_y+V_x+V_y < 2ε (no arc-count K, no cover structure), and V-arcs are anchored at bbox-min phases while U-arcs sit at both bbox extremes — i.e., prove "G+V ≤ 2c forces |∪ bbox-extreme-phase arcs of length O(t₀+√c)| < 2ε", a measure statement strictly weaker than PH(K,λ). **(2)** the b₀-dodge is now quantified: dodging costs b₀ ≥ ε + Γ, and Γ grows with tilt mass on sub-unit squares — combine with T4 (ε ≤ Σdθ, tilt mass mandatory) to try to force Γ > 0 uniformly, pincering (a) against the Wall/α′ jaw which taxes exactly the big-square carriers b₀ counts. Recommended next assignment: attack min(b₀-dodge, phase-scatter) jointly on the enemy profile G+V ≤ 2c using S3 + α′-edge (1/240) + Theorem W, targeting the first unconditional ε ≤ 1/2 − c₀. Also: propagate Lemma A″/S2/S3 into ERDOS_106_REPORT.md §7B as the tilted extension of the capture-side squeeze (they subsume restricted FCMB-AP and sharpen Theorem W's mechanism), and hand S2 to a verifier agent (the one unverified assembly is the T_1/T_{≥2} ledger; everything else reuses [V] components).